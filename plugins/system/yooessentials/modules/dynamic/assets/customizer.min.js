/* Essentials YOOtheme Pro 1.9.3 build 0125.1555; ZOOlanders https://www.zoolanders.com; Copyright (C) Joolanders, SL; http://www.gnu.org/licenses/gpl.html GNU/GPL */

(function (Vue, uikitUtil, _yooessentials, $fields) {
  'use strict';

  function _interopDefaultLegacy (e) { return e && typeof e === 'object' && 'default' in e ? e : { 'default': e }; }

  var Vue__default = /*#__PURE__*/_interopDefaultLegacy(Vue);
  var $fields__default = /*#__PURE__*/_interopDefaultLegacy($fields);

  function _defineProperties(target, props) {
    for (var i = 0; i < props.length; i++) {
      var descriptor = props[i];
      descriptor.enumerable = descriptor.enumerable || false;
      descriptor.configurable = true;
      if ("value" in descriptor) descriptor.writable = true;
      Object.defineProperty(target, _toPropertyKey(descriptor.key), descriptor);
    }
  }
  function _createClass(Constructor, protoProps, staticProps) {
    if (protoProps) _defineProperties(Constructor.prototype, protoProps);
    if (staticProps) _defineProperties(Constructor, staticProps);
    Object.defineProperty(Constructor, "prototype", {
      writable: false
    });
    return Constructor;
  }
  function _extends() {
    _extends = Object.assign ? Object.assign.bind() : function (target) {
      for (var i = 1; i < arguments.length; i++) {
        var source = arguments[i];
        for (var key in source) {
          if (Object.prototype.hasOwnProperty.call(source, key)) {
            target[key] = source[key];
          }
        }
      }
      return target;
    };
    return _extends.apply(this, arguments);
  }
  function _objectWithoutPropertiesLoose(source, excluded) {
    if (source == null) return {};
    var target = {};
    var sourceKeys = Object.keys(source);
    var key, i;
    for (i = 0; i < sourceKeys.length; i++) {
      key = sourceKeys[i];
      if (excluded.indexOf(key) >= 0) continue;
      target[key] = source[key];
    }
    return target;
  }
  function _unsupportedIterableToArray(o, minLen) {
    if (!o) return;
    if (typeof o === "string") return _arrayLikeToArray(o, minLen);
    var n = Object.prototype.toString.call(o).slice(8, -1);
    if (n === "Object" && o.constructor) n = o.constructor.name;
    if (n === "Map" || n === "Set") return Array.from(o);
    if (n === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n)) return _arrayLikeToArray(o, minLen);
  }
  function _arrayLikeToArray(arr, len) {
    if (len == null || len > arr.length) len = arr.length;
    for (var i = 0, arr2 = new Array(len); i < len; i++) arr2[i] = arr[i];
    return arr2;
  }
  function _createForOfIteratorHelperLoose(o, allowArrayLike) {
    var it = typeof Symbol !== "undefined" && o[Symbol.iterator] || o["@@iterator"];
    if (it) return (it = it.call(o)).next.bind(it);
    if (Array.isArray(o) || (it = _unsupportedIterableToArray(o)) || allowArrayLike && o && typeof o.length === "number") {
      if (it) o = it;
      var i = 0;
      return function () {
        if (i >= o.length) return {
          done: true
        };
        return {
          done: false,
          value: o[i++]
        };
      };
    }
    throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
  }
  function _toPrimitive(input, hint) {
    if (typeof input !== "object" || input === null) return input;
    var prim = input[Symbol.toPrimitive];
    if (prim !== undefined) {
      var res = prim.call(input, hint || "default");
      if (typeof res !== "object") return res;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return (hint === "string" ? String : Number)(input);
  }
  function _toPropertyKey(arg) {
    var key = _toPrimitive(arg, "string");
    return typeof key === "symbol" ? key : String(key);
  }

  var SourcePanel$2 = {
    "extends": _yooessentials.FieldsPanel,
    provide: function provide() {
      return {
        $node: this.node
      };
    },
    props: {
      node: {
        type: Object
      },
      Source: {
        type: Object,
        "default": function _default() {
          return _yooessentials.Source;
        }
      }
    },
    methods: {
      prepare: function prepare(config) {
        var _this = this;
        return _yooessentials.FieldsPanel.methods.prepare.call(this, config).map(function (field) {
          _this.$trigger('yooessentialsBuilderPrepareField', {
            node: _this.node,
            field: field
          });
          return field;
        });
      }
    }
  };
  var $customizer$1 = new Proxy({}, {
    get: function get(target, prop) {
      var _window$$yooessential, _window$$yooessential2;
      return (_window$$yooessential = window.$yooessentials) == null ? void 0 : (_window$$yooessential2 = _window$$yooessential.yoocustomizer) == null ? void 0 : _window$$yooessential2[prop];
    }
  });
  var script$a = {
    name: 'QueryPanel',
    components: {
      SourcePanel: SourcePanel$2
    },
    inject: ['Sidebar'],
    props: {
      queryId: {
        type: String,
        required: true
      },
      builder: {
        type: Object
      }
    },
    computed: {
      queryObject: function queryObject() {
        return _yooessentials.Dynamic.getGlobalQuery(this.queryId);
      },
      panel: function panel() {
        var _$customizer$panels;
        return ($customizer$1 == null ? void 0 : (_$customizer$panels = $customizer$1.panels) == null ? void 0 : _$customizer$panels['yooessentials-source-query-global']) || {};
      }
    },
    created: function created() {
      var _this2 = this;
      var stop = _yooessentials.Dynamic.$watch('queries', function () {
        _yooessentials.Dynamic.$nextTick(function () {
          if (!_yooessentials.Dynamic.getGlobalQuery(_this2.queryId)) {
            _this2.Sidebar.closePanel();
          }
        });
        stop();
      });
    },
    events: {
      openPanel: {
        handler: function handler(_ref, name) {
          var params = _ref.params;
          params[0].component = SourcePanel$2;
        },
        priority: 5
      },
      'yooessentials-prerequest': function yooessentialsPrerequest(_ref2) {
        var _params$0$form, _params$, _this$Sidebar, _this$Sidebar$panel, _this$Sidebar$panel$p;
        var params = _ref2.params;
        var form = (_params$0$form = params == null ? void 0 : (_params$ = params[0]) == null ? void 0 : _params$.form) != null ? _params$0$form : {};
        params[0].form = _extends({}, form, (_this$Sidebar = this.Sidebar) == null ? void 0 : (_this$Sidebar$panel = _this$Sidebar.panel) == null ? void 0 : (_this$Sidebar$panel$p = _this$Sidebar$panel.props) == null ? void 0 : _this$Sidebar$panel$p.values);
      }
    }
  };

  function normalizeComponent(template, style, script, scopeId, isFunctionalTemplate, moduleIdentifier /* server only */, shadowMode, createInjector, createInjectorSSR, createInjectorShadow) {
      if (typeof shadowMode !== 'boolean') {
          createInjectorSSR = createInjector;
          createInjector = shadowMode;
          shadowMode = false;
      }
      // Vue.extend constructor export interop.
      const options = typeof script === 'function' ? script.options : script;
      // render functions
      if (template && template.render) {
          options.render = template.render;
          options.staticRenderFns = template.staticRenderFns;
          options._compiled = true;
          // functional template
          if (isFunctionalTemplate) {
              options.functional = true;
          }
      }
      // scopedId
      if (scopeId) {
          options._scopeId = scopeId;
      }
      let hook;
      if (moduleIdentifier) {
          // server build
          hook = function (context) {
              // 2.3 injection
              context =
                  context || // cached call
                      (this.$vnode && this.$vnode.ssrContext) || // stateful
                      (this.parent && this.parent.$vnode && this.parent.$vnode.ssrContext); // functional
              // 2.2 with runInNewContext: true
              if (!context && typeof __VUE_SSR_CONTEXT__ !== 'undefined') {
                  context = __VUE_SSR_CONTEXT__;
              }
              // inject component styles
              if (style) {
                  style.call(this, createInjectorSSR(context));
              }
              // register component module identifier for async chunk inference
              if (context && context._registeredComponents) {
                  context._registeredComponents.add(moduleIdentifier);
              }
          };
          // used by ssr in case component is cached and beforeCreate
          // never gets called
          options._ssrRegister = hook;
      }
      else if (style) {
          hook = shadowMode
              ? function (context) {
                  style.call(this, createInjectorShadow(context, this.$root.$options.shadowRoot));
              }
              : function (context) {
                  style.call(this, createInjector(context));
              };
      }
      if (hook) {
          if (options.functional) {
              // register for functional component in vue file
              const originalRender = options.render;
              options.render = function renderWithStyleInjection(h, context) {
                  hook.call(context);
                  return originalRender(h, context);
              };
          }
          else {
              // inject component registration as beforeCreate hook
              const existing = options.beforeCreate;
              options.beforeCreate = existing ? [].concat(existing, hook) : [hook];
          }
      }
      return script;
  }

  /* script */
  var __vue_script__$a = script$a;

  /* template */
  var __vue_render__$a = function __vue_render__() {
    var _vm = this;
    var _h = _vm.$createElement;
    var _c = _vm._self._c || _h;
    return !_vm.queryObject ? _c('div', [_vm._v("\n    Query Not Found\n")]) : _c('SourcePanel', {
      attrs: {
        "node": _vm.queryObject,
        "panel": _vm.panel,
        "values": _vm.queryObject
      }
    });
  };
  var __vue_staticRenderFns__$a = [];

  /* style */
  var __vue_inject_styles__$a = undefined;
  /* scoped */
  var __vue_scope_id__$a = undefined;
  /* module identifier */
  var __vue_module_identifier__$a = undefined;
  /* functional template */
  var __vue_is_functional_template__$a = false;
  /* style inject */

  /* style inject SSR */

  /* style inject shadow dom */

  var __vue_component__$a = /*#__PURE__*/normalizeComponent({
    render: __vue_render__$a,
    staticRenderFns: __vue_staticRenderFns__$a
  }, __vue_inject_styles__$a, __vue_script__$a, __vue_scope_id__$a, __vue_is_functional_template__$a, __vue_module_identifier__$a, false, undefined, undefined, undefined);

  //
  var SourcePanel$1 = {
    "extends": _yooessentials.BuilderPanel,
    props: {
      Source: {
        type: Object,
        "default": function _default() {
          return _yooessentials.Source;
        }
      }
    }
  };
  var script$9 = {
    name: 'NewQueryModal',
    components: {
      SourcePanel: SourcePanel$1
    },
    inject: {
      Builder: {
        "default": function _default() {
          return window.$yooess.yoobuilder;
        }
      }
    },
    data: function data() {
      return {
        values: {},
        error: ''
      };
    },
    computed: {
      validated: function validated() {
        var _this$values, _this$values$source, _this$values$source$q;
        return this.values.name && ((_this$values = this.values) == null ? void 0 : (_this$values$source = _this$values.source) == null ? void 0 : (_this$values$source$q = _this$values$source.query) == null ? void 0 : _this$values$source$q.name);
      },
      panel: function panel() {
        return {
          title: 'Source Query',
          fields: {
            name: {
              label: 'Name',
              description: 'A name to identify this query.',
              attrs: {
                autofocus: true
              }
            },
            _source: {
              label: 'Source',
              type: 'yooessentials-source-select',
              description: 'Select a content source for which to make a query.',
              omitgroups: ['', 'Page', 'Global'],
              attrs: {
                required: true
              }
            }
          }
        };
      }
    },
    methods: {
      isEmpty: uikitUtil.isEmpty,
      save: function save() {
        this.$emit('resolve', this.values);
      }
    }
  };

  /* script */
  var __vue_script__$9 = script$9;

  /* template */
  var __vue_render__$9 = function __vue_render__() {
    var _vm = this;
    var _h = _vm.$createElement;
    var _c = _vm._self._c || _h;
    return _c('form', {
      on: {
        "submit": function submit($event) {
          $event.preventDefault();
          return _vm.save.apply(null, arguments);
        }
      }
    }, [_vm._t("header", function () {
      return [_c('div', {
        staticClass: "uk-modal-header uk-flex uk-flex-middle uk-flex-between"
      }, [_c('h3', {
        staticClass: "uk-margin-remove"
      }, [_vm._v(_vm._s(_vm.$t('Source Query')))])])];
    }), _vm._v(" "), _c('div', {
      staticClass: "uk-modal-body"
    }, [_c('SourcePanel', {
      attrs: {
        "node": _vm.values,
        "builder": _vm.Builder,
        "panel": _vm.panel,
        "values": _vm.values
      }
    })], 1), _vm._v(" "), _c('div', {
      staticClass: "uk-modal-footer uk-flex uk-flex-middle uk-flex-right"
    }, [_c('button', {
      staticClass: "uk-button uk-button-text uk-modal-close uk-margin-right",
      attrs: {
        "type": "button"
      }
    }, [_vm._v(_vm._s(_vm.$t('Cancel')))]), _vm._v(" "), _c('button', {
      staticClass: "uk-button uk-button-primary",
      attrs: {
        "disabled": !_vm.validated
      }
    }, [_vm._v(_vm._s(_vm.$t('Save')))])])], 2);
  };
  var __vue_staticRenderFns__$9 = [];

  /* style */
  var __vue_inject_styles__$9 = undefined;
  /* scoped */
  var __vue_scope_id__$9 = undefined;
  /* module identifier */
  var __vue_module_identifier__$9 = undefined;
  /* functional template */
  var __vue_is_functional_template__$9 = false;
  /* style inject */

  /* style inject SSR */

  /* style inject shadow dom */

  var __vue_component__$9 = /*#__PURE__*/normalizeComponent({
    render: __vue_render__$9,
    staticRenderFns: __vue_staticRenderFns__$9
  }, __vue_inject_styles__$9, __vue_script__$9, __vue_scope_id__$9, __vue_is_functional_template__$9, __vue_module_identifier__$9, false, undefined, undefined, undefined);

  var _excluded$1 = ["id"];
  var script$8 = {
    name: 'SourceQueriesSection',
    props: {
      panel: Object
    },
    computed: {
      queries: function queries() {
        return _yooessentials.Dynamic.queries;
      }
    },
    methods: {
      isEmpty: uikitUtil.isEmpty,
      getQueryLabel: function getQueryLabel(query) {
        var _SourceExtended$schem, _query$source, _query$source$query;
        return ((_SourceExtended$schem = _yooessentials.SourceExtended.schema.findField((query == null ? void 0 : (_query$source = query.source) == null ? void 0 : (_query$source$query = _query$source.query) == null ? void 0 : _query$source$query.name) || '')) == null ? void 0 : _SourceExtended$schem.metadata.label) || 'Unknown';
      },
      add: function add() {
        var _this = this;
        this.$modal(__vue_component__$9).show().then(function (query) {
          if (!query) {
            return;
          }
          query = _yooessentials.Dynamic.saveGlobalQuery(query);
          _this.edit(query);
        });
      },
      edit: function edit(query) {
        this.$trigger('openPanel', {
          width: 450,
          name: 'yooessentials-source-query',
          title: 'Source Query',
          component: __vue_component__$a,
          props: {
            queryId: query.id
          }
        });
      },
      duplicate: function duplicate(_ref) {
        _ref.id;
          var query = _objectWithoutPropertiesLoose(_ref, _excluded$1);
        query.name = query.name ? query.name + " Copy" : '';
        _yooessentials.Dynamic.saveGlobalQuery(_yooessentials.deepCopy(query));
      },
      remove: function remove(query) {
        _yooessentials.Dynamic.removeGlobalQuery(query);
      }
    }
  };

  /* script */
  var __vue_script__$8 = script$8;

  /* template */
  var __vue_render__$8 = function __vue_render__() {
    var _vm = this;
    var _h = _vm.$createElement;
    var _c = _vm._self._c || _h;
    return _c('div', {
      staticClass: "uk-margin-remove-first-child"
    }, [!_vm.isEmpty(_vm.queries) ? _c('ul', {
      staticClass: "uk-nav uk-nav-default yo-sidebar-marginless uk-text-capitalize yo-nav-iconnav"
    }, _vm._l(_vm.queries, function (query) {
      return _c('li', {
        key: query.id,
        staticClass: "uk-visible-toggle"
      }, [_c('a', {
        staticClass: "uk-c",
        attrs: {
          "href": ""
        },
        on: {
          "click": function click($event) {
            $event.preventDefault();
            return _vm.edit(query);
          }
        }
      }, [_c('div', {
        attrs: {
          "lass": "uk-flex"
        }
      }, [_c('span', [_vm._v(_vm._s(query.name || _vm.getQueryLabel(query)))]), _vm._v(" "), query.name ? _c('div', {
        staticClass: "uk-text-meta"
      }, [_vm._v(_vm._s(_vm.getQueryLabel(query)))]) : _vm._e()])]), _vm._v(" "), _c('div', {
        staticClass: "uk-invisible-hover uk-position-center-right uk-position-medium"
      }, [_c('ul', {
        staticClass: "uk-iconnav uk-flex-nowrap"
      }, [_c('li', [_c('a', {
        staticClass: "uk-icon-link uk-preserve-width",
        attrs: {
          "href": "",
          "title": _vm.$t('Copy'),
          "uk-icon": "copy",
          "uk-tooltip": "delay: 500"
        },
        on: {
          "click": function click($event) {
            $event.preventDefault();
            return _vm.duplicate(query);
          }
        }
      })]), _vm._v(" "), _c('li', [_c('a', {
        staticClass: "uk-icon-link uk-preserve-width",
        attrs: {
          "href": "",
          "title": _vm.$t('Delete'),
          "uk-icon": "trash",
          "uk-tooltip": "delay: 500"
        },
        on: {
          "click": function click($event) {
            $event.preventDefault();
            return _vm.remove(query);
          }
        }
      })])])])]);
    }), 0) : _vm._e(), _vm._v(" "), _c('button', {
      staticClass: "uk-button uk-button-default uk-width-1-1",
      "class": {
        'uk-margin-medium-top': !_vm.isEmpty(_vm.queries)
      },
      attrs: {
        "type": "button"
      },
      on: {
        "click": function click($event) {
          $event.preventDefault();
          return _vm.add.apply(null, arguments);
        }
      }
    }, [_vm._v(_vm._s(_vm.$t('New Query')))]), _vm._v(" "), _c('p', {
      staticClass: "uk-text-muted"
    }, [_vm._v(_vm._s(_vm.$t('Preset Source Queries for the Builder Dynamic Content workflow.')))])]);
  };
  var __vue_staticRenderFns__$8 = [];

  /* style */
  var __vue_inject_styles__$8 = undefined;
  /* scoped */
  var __vue_scope_id__$8 = undefined;
  /* module identifier */
  var __vue_module_identifier__$8 = undefined;
  /* functional template */
  var __vue_is_functional_template__$8 = false;
  /* style inject */

  /* style inject SSR */

  /* style inject shadow dom */

  var __vue_component__$8 = /*#__PURE__*/normalizeComponent({
    render: __vue_render__$8,
    staticRenderFns: __vue_staticRenderFns__$8
  }, __vue_inject_styles__$8, __vue_script__$8, __vue_scope_id__$8, __vue_is_functional_template__$8, __vue_module_identifier__$8, false, undefined, undefined, undefined);

  //
  //
  //
  //
  //
  //
  //
  //

  var script$7 = {
    name: 'SourceExtendedIcon',
    props: {
      node: {
        type: Object,
        required: true
      },
      error: {
        type: [String, Boolean]
      }
    },
    computed: {
      icon: function icon() {
        return this.error ? 'dynamic-error' : 'dynamic';
      },
      title: function title() {
        return this.error || this.$t('Dynamic Content');
      },
      position: function position() {
        var positions = {
          section: 'top',
          row: 'left'
        };
        return positions[this.node.type.name] || 'bottom';
      }
    }
  };

  /* script */
  var __vue_script__$7 = script$7;

  /* template */
  var __vue_render__$7 = function __vue_render__() {
    var _vm = this;
    var _h = _vm.$createElement;
    var _c = _vm._self._c || _h;
    return _c('span', {
      "class": "yo-builder-icon-" + _vm.icon,
      attrs: {
        "title": _vm.$t(_vm.title),
        "uk-tooltip": "pos: " + _vm.position
      }
    });
  };
  var __vue_staticRenderFns__$7 = [];

  /* style */
  var __vue_inject_styles__$7 = undefined;
  /* scoped */
  var __vue_scope_id__$7 = undefined;
  /* module identifier */
  var __vue_module_identifier__$7 = undefined;
  /* functional template */
  var __vue_is_functional_template__$7 = false;
  /* style inject */

  /* style inject SSR */

  /* style inject shadow dom */

  var __vue_component__$7 = /*#__PURE__*/normalizeComponent({
    render: __vue_render__$7,
    staticRenderFns: __vue_staticRenderFns__$7
  }, __vue_inject_styles__$7, __vue_script__$7, __vue_scope_id__$7, __vue_is_functional_template__$7, __vue_module_identifier__$7, false, undefined, undefined, undefined);

  /*!
   * vue-fields v1.1.3
   * https://github.com/pagekit/vue-fields
   * Released under the MIT License.
   */

  /**
   * Utility functions.
   */
  var _config = {},
    _set;
  var assign = Object.assign || _assign;
  var isArray$2 = Array.isArray;
  function Util(_ref) {
    var set = _ref.set,
      config = _ref.config;
    _set = set;
    _config = config;
  }
  function log(message, color) {
    if (color === void 0) {
      color = '#41B883';
    }
    if (typeof console !== 'undefined' && _config.devtools) {
      console.log("%c vue-fields %c " + message + " ", 'color: #fff; background: #35495E; padding: 1px; border-radius: 3px 0 0 3px;', "color: #fff; background: " + color + "; padding: 1px; border-radius: 0 3px 3px 0;");
    }
  }
  function warn(message, color) {
    if (color === void 0) {
      color = '#DB6B00';
    }
    log(message, color);
  }
  function isObject$1(obj) {
    return obj !== null && typeof obj === 'object';
  }
  function isString(val) {
    return typeof val === 'string';
  }
  function isFunction$1(val) {
    return typeof val === 'function';
  }
  function isUndefined(val) {
    return typeof val === 'undefined';
  }
  function get$1(obj, key, def) {
    var parts = isArray$2(key) ? key : key.split('.');
    for (var i = 0; i < parts.length; i++) {
      if (isObject$1(obj) && !isUndefined(obj[parts[i]])) {
        obj = obj[parts[i]];
      } else {
        return def;
      }
    }
    return obj;
  }
  function set(obj, key, val) {
    var parts = isArray$2(key) ? key : key.split('.');
    while (parts.length > 1) {
      var part = parts.shift();
      if (!isObject$1(obj[part])) {
        _set(obj, part, {});
      }
      obj = obj[part];
    }
    _set(obj, parts.shift(), val);
  }
  var parsedFunc = {};
  var expressionRe = /((?:\d|true|false|null|undefined|(?:this\.|\$)[\w.$]+|\W)*)([\w][\w.-]*)?/g;
  var quotedStringRe = /([^"']+)((.)(?:[^\3\\]|\\.)*?\3|.)?/g;
  function parse(expr) {
    return parsedFunc[expr] = parsedFunc[expr] || Function('$values', '$context', "with($context){return " + expr.replace(quotedStringRe, function (match, unquoted, quoted) {
      if (quoted === void 0) {
        quoted = '';
      }
      return unquoted.replace(expressionRe, function (match, prefix, expression) {
        if (prefix === void 0) {
          prefix = '';
        }
        return match ? "" + prefix + (expression ? "$get('" + expression + "')" : '') : '';
      }) + quoted;
    }) + "}");
  }
  function each(obj, iterator) {
    var i, key;
    if (typeof obj.length == 'number') {
      for (i = 0; i < obj.length; i++) {
        iterator.call(obj[i], obj[i], i);
      }
    } else if (isObject$1(obj)) {
      for (key in obj) {
        if (obj.hasOwnProperty(key)) {
          iterator.call(obj[key], obj[key], key);
        }
      }
    }
    return obj;
  }
  /**
   * Object.assign() polyfill.
   */

  function _assign(target) {
    for (var _len = arguments.length, sources = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
      sources[_key - 1] = arguments[_key];
    }
    sources.forEach(function (source) {
      Object.keys(source || {}).forEach(function (key) {
        return target[key] = source[key];
      });
    });
    return target;
  }
  var Field = {
    inject: ['Fields'],
    props: {
      field: {
        type: Object,
        required: true
      },
      values: {
        type: Object,
        required: true
      }
    },
    data: function data() {
      return assign({
        name: '',
        label: '',
        attrs: {},
        options: [],
        "default": undefined
      }, this.field);
    },
    computed: {
      value: {
        get: function get$$1() {
          return get$1(this.values, this.name);
        },
        set: function set$$1(value) {
          this.$emit('change', value, this);
        }
      },
      attributes: {
        get: function get$$1() {
          if (!this.Fields.evaluate(this.enable)) {
            return assign({
              disabled: 'true'
            }, this.attrs);
          }
          return this.attrs;
        }
      }
    },
    created: function created() {
      if (isUndefined(this.value) && !isUndefined(this["default"])) {
        this.value = this["default"];
      }
    },
    methods: {
      filterOptions: function filterOptions(options) {
        var _this = this;
        var opts = [];
        if (!options) {
          warn("Invalid options provided for " + this.name);
          return opts;
        }
        each(options, function (value, name) {
          if (isObject$1(value)) {
            opts.push({
              label: name,
              options: _this.filterOptions(value)
            });
          } else {
            opts.push({
              text: name,
              value: value
            });
          }
        });
        return opts;
      }
    }
  };
  var FieldText = {
    render: function render() {
      var _vm = this;
      var _h = _vm.$createElement;
      var _c = _vm._self._c || _h;
      return _c('input', _vm._b({
        directives: [{
          name: "model",
          rawName: "v-model",
          value: _vm.value,
          expression: "value"
        }],
        attrs: {
          "type": "text"
        },
        domProps: {
          "value": _vm.value
        },
        on: {
          "input": function input($event) {
            if ($event.target.composing) {
              return;
            }
            _vm.value = $event.target.value;
          }
        }
      }, 'input', _vm.attributes, false));
    },
    staticRenderFns: [],
    "extends": Field
  };
  var FieldTextarea = {
    render: function render() {
      var _vm = this;
      var _h = _vm.$createElement;
      var _c = _vm._self._c || _h;
      return _c('textarea', _vm._b({
        directives: [{
          name: "model",
          rawName: "v-model",
          value: _vm.value,
          expression: "value"
        }],
        domProps: {
          "value": _vm.value
        },
        on: {
          "input": function input($event) {
            if ($event.target.composing) {
              return;
            }
            _vm.value = $event.target.value;
          }
        }
      }, 'textarea', _vm.attributes, false));
    },
    staticRenderFns: [],
    "extends": Field
  };
  var FieldRadio = {
    render: function render() {
      var _vm = this;
      var _h = _vm.$createElement;
      var _c = _vm._self._c || _h;
      return _c('div', [_vm._l(_vm.filterOptions(_vm.options), function (option) {
        return [_c('input', _vm._b({
          directives: [{
            name: "model",
            rawName: "v-model",
            value: _vm.value,
            expression: "value"
          }],
          attrs: {
            "name": _vm.name,
            "type": "radio"
          },
          domProps: {
            "value": option.value,
            "checked": _vm._q(_vm.value, option.value)
          },
          on: {
            "change": function change($event) {
              _vm.value = option.value;
            }
          }
        }, 'input', _vm.attributes, false)), _vm._v(" "), _c('label', [_vm._v(_vm._s(option.text))])];
      })], 2);
    },
    staticRenderFns: [],
    "extends": Field
  };
  var FieldCheckbox = {
    render: function render() {
      var _vm = this;
      var _h = _vm.$createElement;
      var _c = _vm._self._c || _h;
      return _c('input', _vm._b({
        directives: [{
          name: "model",
          rawName: "v-model",
          value: _vm.value,
          expression: "value"
        }],
        attrs: {
          "type": "checkbox"
        },
        domProps: {
          "checked": Array.isArray(_vm.value) ? _vm._i(_vm.value, null) > -1 : _vm.value
        },
        on: {
          "change": function change($event) {
            var $$a = _vm.value,
              $$el = $event.target,
              $$c = $$el.checked ? true : false;
            if (Array.isArray($$a)) {
              var $$v = null,
                $$i = _vm._i($$a, $$v);
              if ($$el.checked) {
                $$i < 0 && (_vm.value = $$a.concat([$$v]));
              } else {
                $$i > -1 && (_vm.value = $$a.slice(0, $$i).concat($$a.slice($$i + 1)));
              }
            } else {
              _vm.value = $$c;
            }
          }
        }
      }, 'input', _vm.attributes, false));
    },
    staticRenderFns: [],
    "extends": Field
  };
  var FieldSelect = {
    render: function render() {
      var _vm = this;
      var _h = _vm.$createElement;
      var _c = _vm._self._c || _h;
      return _c('select', _vm._b({
        directives: [{
          name: "model",
          rawName: "v-model",
          value: _vm.value,
          expression: "value"
        }],
        on: {
          "change": function change($event) {
            var $$selectedVal = Array.prototype.filter.call($event.target.options, function (o) {
              return o.selected;
            }).map(function (o) {
              var val = "_value" in o ? o._value : o.value;
              return val;
            });
            _vm.value = $event.target.multiple ? $$selectedVal : $$selectedVal[0];
          }
        }
      }, 'select', _vm.attributes, false), [_vm._l(_vm.filterOptions(_vm.options), function (option) {
        return [option.label ? _c('optgroup', {
          attrs: {
            "label": option.label
          }
        }, _vm._l(option.options, function (opt) {
          return _c('option', {
            domProps: {
              "value": opt.value
            }
          }, [_vm._v(_vm._s(opt.text))]);
        }), 0) : _c('option', {
          domProps: {
            "value": option.value
          }
        }, [_vm._v(_vm._s(option.text))])];
      })], 2);
    },
    staticRenderFns: [],
    "extends": Field
  };
  var FieldRange = {
    render: function render() {
      var _vm = this;
      var _h = _vm.$createElement;
      var _c = _vm._self._c || _h;
      return _c('input', _vm._b({
        directives: [{
          name: "model",
          rawName: "v-model",
          value: _vm.value,
          expression: "value"
        }],
        attrs: {
          "type": "range"
        },
        domProps: {
          "value": _vm.value
        },
        on: {
          "__r": function __r($event) {
            _vm.value = $event.target.value;
          }
        }
      }, 'input', _vm.attributes, false));
    },
    staticRenderFns: [],
    "extends": Field
  };
  var FieldNumber = {
    render: function render() {
      var _vm = this;
      var _h = _vm.$createElement;
      var _c = _vm._self._c || _h;
      return _c('input', _vm._b({
        directives: [{
          name: "model",
          rawName: "v-model",
          value: _vm.value,
          expression: "value"
        }],
        attrs: {
          "type": "number"
        },
        domProps: {
          "value": _vm.value
        },
        on: {
          "input": function input($event) {
            if ($event.target.composing) {
              return;
            }
            _vm.value = $event.target.value;
          }
        }
      }, 'input', _vm.attributes, false));
    },
    staticRenderFns: [],
    "extends": Field
  };
  var Fields = {
    render: function render() {
      var _vm = this;
      var _h = _vm.$createElement;
      var _c = _vm._self._c || _h;
      return _c('div', _vm._l(_vm.fields, function (field) {
        return _c('div', {
          directives: [{
            name: "show",
            rawName: "v-show",
            value: _vm.evaluate(field.show),
            expression: "evaluate(field.show)"
          }],
          key: field.name
        }, [field.type != 'checkbox' ? _c('label', [_vm._v(_vm._s(field.label))]) : _vm._e(), _vm._v(" "), _c(field.component, {
          tag: "component",
          attrs: {
            "field": field,
            "values": _vm.values
          },
          on: {
            "change": _vm.change
          }
        })], 1);
      }), 0);
    },
    staticRenderFns: [],
    components: {
      FieldText: FieldText,
      FieldTextarea: FieldTextarea,
      FieldRadio: FieldRadio,
      FieldCheckbox: FieldCheckbox,
      FieldSelect: FieldSelect,
      FieldRange: FieldRange,
      FieldNumber: FieldNumber
    },
    provide: function provide() {
      return {
        Fields: this
      };
    },
    props: {
      config: {
        type: [Object, Array],
        "default": function _default() {
          return {};
        }
      },
      values: {
        type: Object,
        "default": function _default() {
          return {};
        }
      },
      prefix: {
        type: String,
        "default": 'field-'
      }
    },
    computed: {
      fields: function fields() {
        return this.prepare();
      }
    },
    methods: {
      change: function change(value, field) {
        set(this.values, field.name, value);
        this.$emit('change', value, field);
      },
      prepare: function prepare(config, prefix) {
        if (config === void 0) {
          config = this.config;
        }
        if (prefix === void 0) {
          prefix = this.prefix;
        }
        var arr = isArray$2(config),
          fields = [];
        each(config, function (field, key) {
          field = assign({}, field);
          if (!field.name && !arr) {
            field.name = key;
          }
          if (field.name) {
            if (!field.type) {
              field.type = 'text';
            }
            if (!field.component) {
              field.component = prefix + field.type;
            }
            fields.push(field);
          } else {
            warn("Field name missing " + JSON.stringify(field));
          }
        });
        return fields;
      },
      evaluate: function evaluate(expression, values) {
        if (values === void 0) {
          values = this.values;
        }
        try {
          if (isUndefined(expression)) {
            return true;
          }
          if (isString(expression)) {
            expression = parse(expression);
          }
          if (isFunction$1(expression)) {
            return expression.call(this, values, {
              $match: $match,
              $get: function $get(key) {
                return get$1(values, key);
              }
            });
          }
          return expression;
        } catch (e) {
          warn(e);
        }
        return true;
      }
    }
  };
  function $match(subject, pattern, flags) {
    return subject && new RegExp(pattern, flags).test(subject);
  }

  /**
   * Install plugin.
   */
  var Plugin = {
    Field: Field,
    Fields: Fields,
    install: function install(Vue) {
      if (this.installed) {
        return;
      }
      Util(Vue);
      log(this.version);
      Vue.component('field', Field);
      Vue.component('fields', Fields);
    },
    version: '1.1.3'
  };
  if (typeof window !== 'undefined' && window.Vue) {
    window.Vue.use(Plugin);
  }

  //
  var script$6 = {
    "extends": {
      // required so the fields ref is kept
      components: $fields__default["default"],
      "extends": Fields
    },
    methods: {
      assign: uikitUtil.assign
    }
  };

  /* script */
  var __vue_script__$6 = script$6;

  /* template */
  var __vue_render__$6 = function __vue_render__() {
    var _vm = this;
    var _h = _vm.$createElement;
    var _c = _vm._self._c || _h;
    return _c('div', {
      staticClass: "uk-panel"
    }, _vm._l(_vm.fields, function (field) {
      return _c('div', {
        directives: [{
          name: "show",
          rawName: "v-show",
          value: _vm.evaluate(field.show),
          expression: "evaluate(field.show)"
        }],
        key: field.name,
        staticClass: "uk-flex uk-flex-middle uk-flex-wrap uk-margin-small"
      }, [_c('div', {
        staticClass: "uk-width-expand uk-text-truncate"
      }, [field.description ? _c('span', {
        attrs: {
          "title": field.description,
          "uk-tooltip": "delay: 300; pos: top-left"
        }
      }, [_vm._v(_vm._s(_vm.$t(field.label || '')))]) : _c('span', [_vm._v(_vm._s(_vm.$t(field.label || '')))])]), _vm._v(" "), _c('div', {
        staticClass: "uk-width-medium"
      }, [_c(field.component, {
        tag: "component",
        attrs: {
          "values": _vm.values,
          "field": _vm.assign(field, {
            attrs: {
              "class": 'yo-form-medium'
            }
          })
        },
        on: {
          "change": _vm.change
        }
      })], 1)]);
    }), 0);
  };
  var __vue_staticRenderFns__$6 = [];

  /* style */
  var __vue_inject_styles__$6 = undefined;
  /* scoped */
  var __vue_scope_id__$6 = undefined;
  /* module identifier */
  var __vue_module_identifier__$6 = undefined;
  /* functional template */
  var __vue_is_functional_template__$6 = false;
  /* style inject */

  /* style inject SSR */

  /* style inject shadow dom */

  var __vue_component__$6 = /*#__PURE__*/normalizeComponent({
    render: __vue_render__$6,
    staticRenderFns: __vue_staticRenderFns__$6
  }, __vue_inject_styles__$6, __vue_script__$6, __vue_scope_id__$6, __vue_is_functional_template__$6, __vue_module_identifier__$6, false, undefined, undefined, undefined);

  //
  var script$5 = {
    components: {
      FieldsPanel: __vue_component__$6
    },
    props: {
      fields: {
        type: Array
      },
      prop: {
        type: Object,
        required: true
      },
      isMulti: {
        type: Boolean,
        "default": false
      }
    },
    computed: {
      fieldsets: function fieldsets() {
        var _this = this;
        return this.fields.filter(function (fieldset) {
          return fieldset.fields.length && (!fieldset.multi || fieldset.multi && _this.isMulti);
        });
      }
    }
  };

  /* script */
  var __vue_script__$5 = script$5;

  /* template */
  var __vue_render__$5 = function __vue_render__() {
    var _vm = this;
    var _h = _vm.$createElement;
    var _c = _vm._self._c || _h;
    return _c('div', _vm._l(_vm.fieldsets, function (fieldset) {
      return _c('div', {
        key: fieldset.label,
        staticClass: "uk-margin"
      }, [_c('h3', {
        staticClass: "yo-sidebar-subheading"
      }, [_vm._v(_vm._s(_vm.$t(fieldset.label)))]), _vm._v(" "), _c('FieldsPanel', {
        attrs: {
          "config": fieldset.fields,
          "values": _vm.prop
        }
      }), _vm._v(" "), fieldset.description ? _c('div', {
        staticClass: "uk-text-meta uk-margin-top"
      }, [_vm._v(_vm._s(_vm.$t(fieldset.description)))]) : _vm._e()], 1);
    }), 0);
  };
  var __vue_staticRenderFns__$5 = [];

  /* style */
  var __vue_inject_styles__$5 = undefined;
  /* scoped */
  var __vue_scope_id__$5 = undefined;
  /* module identifier */
  var __vue_module_identifier__$5 = undefined;
  /* functional template */
  var __vue_is_functional_template__$5 = false;
  /* style inject */

  /* style inject SSR */

  /* style inject shadow dom */

  var __vue_component__$5 = /*#__PURE__*/normalizeComponent({
    render: __vue_render__$5,
    staticRenderFns: __vue_staticRenderFns__$5
  }, __vue_inject_styles__$5, __vue_script__$5, __vue_scope_id__$5, __vue_is_functional_template__$5, __vue_module_identifier__$5, false, undefined, undefined, undefined);

  //
  var script$4 = {
    name: 'DynamicField',
    inject: {
      $node: {}
    },
    props: {
      field: {
        type: Object,
        required: true
      },
      values: Object
    },
    computed: {
      prop: function prop() {
        return _yooessentials.SourceExtended.getProp(this.$node, this.field.name);
      },
      query: function query() {
        return _yooessentials.SourceExtended.getQuery(this.$node, this.field.name);
      },
      config: function config() {
        var _this$prop;
        return _yooessentials.SourceExtended.getPropField(this.query, (_this$prop = this.prop) == null ? void 0 : _this$prop.name);
      },
      isMultiSource: function isMultiSource() {
        return _yooessentials.SourceExtended.isMultiSource(this.$node, this.query, this.field.name);
      },
      isList: function isList() {
        return this.config.type === 'LIST';
      },
      breadcrumb: function breadcrumb() {
        if (!this.config) {
          return [this.prop.name];
        }
        return [this.isList ? "[" + this.config.text + "]" : this.config.text, this.isMultiSource ? "[" + this.config.group + "]" : this.config.group];
      }
    },
    created: function created() {
      var _window$$yooessential;
      this.$yooconfig = (_window$$yooessential = window.$yooessentials) == null ? void 0 : _window$$yooessential.yoocustomizer;
    },
    methods: {
      edit: function edit() {
        this.$dropdown(__vue_component__$5, {
          prop: this.prop,
          fields: this.config.fieldsets,
          isMulti: this.isMultiSource || this.isList
        }).show(this.$el);
      },
      remove: function remove() {
        _yooessentials.SourceExtended.removeProp(this.$node, this.field.name);
      }
    }
  };

  /* script */
  var __vue_script__$4 = script$4;

  /* template */
  var __vue_render__$4 = function __vue_render__() {
    var _vm = this;
    var _h = _vm.$createElement;
    var _c = _vm._self._c || _h;
    return _c('div', {
      staticClass: "uk-position-relative uk-input yo-input-iconnav-right uk-flex",
      "class": {
        'uk-form-danger': !_vm.config
      }
    }, [_c('img', {
      staticClass: "uk-flex-none uk-margin-small-right",
      attrs: {
        "uk-svg": _vm.$yooconfig.assets + "/images/builder/dynamic-field.svg"
      }
    }), _vm._v(" "), _c('div', {
      staticClass: "yo-input-locked uk-flex-1"
    }, [_c('span', {
      attrs: {
        "title": _vm.breadcrumb.join(' - '),
        "uk-tooltip": "delay: 500"
      }
    }, [_vm._v("\n            " + _vm._s(_vm.breadcrumb.join(' - ')) + "\n        ")])]), _vm._v(" "), _c('div', {
      staticClass: "uk-position-center-right uk-position-small"
    }, [_c('ul', {
      staticClass: "uk-iconnav uk-flex-nowrap"
    }, [_c('li', {
      directives: [{
        name: "show",
        rawName: "v-show",
        value: _vm.config,
        expression: "config"
      }]
    }, [_c('a', {
      staticClass: "uk-icon-link uk-preserve-width",
      attrs: {
        "title": _vm.$t('Edit'),
        "href": "",
        "uk-icon": "pencil",
        "uk-tooltip": "delay: 500"
      },
      on: {
        "click": function click($event) {
          $event.preventDefault();
          return _vm.edit.apply(null, arguments);
        }
      }
    })]), _vm._v(" "), !_vm.field.source_default ? _c('li', [_c('a', {
      staticClass: "uk-icon-link uk-preserve-width",
      attrs: {
        "title": _vm.$t('Delete'),
        "href": "",
        "uk-icon": "trash",
        "uk-tooltip": "delay: 500"
      },
      on: {
        "click": function click($event) {
          $event.preventDefault();
          return _vm.remove.apply(null, arguments);
        }
      }
    })]) : _vm._e()])])]);
  };
  var __vue_staticRenderFns__$4 = [];

  /* style */
  var __vue_inject_styles__$4 = undefined;
  /* scoped */
  var __vue_scope_id__$4 = undefined;
  /* module identifier */
  var __vue_module_identifier__$4 = undefined;
  /* functional template */
  var __vue_is_functional_template__$4 = false;
  /* style inject */

  /* style inject SSR */

  /* style inject shadow dom */

  var __vue_component__$4 = /*#__PURE__*/normalizeComponent({
    render: __vue_render__$4,
    staticRenderFns: __vue_staticRenderFns__$4
  }, __vue_inject_styles__$4, __vue_script__$4, __vue_scope_id__$4, __vue_is_functional_template__$4, __vue_module_identifier__$4, false, undefined, undefined, undefined);

  //
  var script$3 = {
    name: 'DynamicDropdownOptions',
    inject: ['$node'],
    props: {
      options: Array,
      selected: Object
    },
    computed: {
      optionsList: function optionsList() {
        return _yooessentials.groupBy(this.options, 'group');
      }
    }
  };

  /* script */
  var __vue_script__$3 = script$3;

  /* template */
  var __vue_render__$3 = function __vue_render__() {
    var _vm = this;
    var _h = _vm.$createElement;
    var _c = _vm._self._c || _h;
    return _c('div', [_c('div', {
      staticClass: "yo-dropdown-body uk-overflow-auto uk-height-max-large"
    }, [_c('ul', {
      staticClass: "uk-nav uk-dropdown-nav"
    }, [_vm._l(_vm.optionsList, function (opts, group, i) {
      return [group ? _c('li', {
        key: "header-" + group,
        staticClass: "uk-nav-header"
      }, [_vm._v("\n                    " + _vm._s(_vm.$t(group)) + "\n                ")]) : _vm._e(), _vm._v(" "), _vm._l(opts, function (opt) {
        return _c('li', {
          key: opt.name,
          "class": {
            'uk-active': _vm.selected && opt.name === _vm.selected.name
          },
          on: {
            "click": function click($event) {
              $event.preventDefault();
              return _vm.$emit('resolve', opt);
            }
          }
        }, [_c('a', {
          attrs: {
            "href": ""
          }
        }, [_c('div', {
          attrs: {
            "lass": "uk-flex"
          }
        }, [_c('span', [_vm._v(_vm._s(_vm.$t(opt.title)))]), _vm._v(" "), opt.meta ? _c('span', {
          staticClass: "uk-text-meta uk-margin-small-left"
        }, [_vm._v(_vm._s(opt.meta))]) : _vm._e()])])]);
      }), _vm._v(" "), ++i !== Object.keys(_vm.optionsList).length ? _c('li', {
        key: "divider-" + group,
        staticClass: "uk-nav-divider"
      }) : _vm._e()];
    })], 2)])]);
  };
  var __vue_staticRenderFns__$3 = [];

  /* style */
  var __vue_inject_styles__$3 = undefined;
  /* scoped */
  var __vue_scope_id__$3 = undefined;
  /* module identifier */
  var __vue_module_identifier__$3 = undefined;
  /* functional template */
  var __vue_is_functional_template__$3 = false;
  /* style inject */

  /* style inject SSR */

  /* style inject shadow dom */

  var __vue_component__$3 = /*#__PURE__*/normalizeComponent({
    render: __vue_render__$3,
    staticRenderFns: __vue_staticRenderFns__$3
  }, __vue_inject_styles__$3, __vue_script__$3, __vue_scope_id__$3, __vue_is_functional_template__$3, __vue_module_identifier__$3, false, undefined, undefined, undefined);

  var _excluded = ["value", "enforce"];
  var script$2 = {
    name: 'DynamicDropdown',
    components: {
      DropdownList: _yooessentials.DropdownList,
      DynamicDropdownOptions: __vue_component__$3
    },
    inject: ['Builder'],
    provide: function provide() {
      return {
        $node: this.node,
        $field: this.field
      };
    },
    props: {
      field: {
        type: Object,
        required: true
      },
      node: {
        type: Object,
        required: true
      }
    },
    data: function data() {
      return {
        isSelected: false,
        selectedOption: null
      };
    },
    computed: {
      prop: function prop() {
        return _yooessentials.SourceExtended.getProp(this.node, this.field.name);
      },
      dynamicOptions: function dynamicOptions() {
        return this.$trigger('yooessentialsDynamicOptions', {
          node: this.node,
          field: this.field
        }) || [];
      },
      matchedOption: function matchedOption() {
        var _this = this;
        return this.dynamicOptions.find(function (opt) {
          return opt == null ? void 0 : opt.match({
            node: _this.node,
            field: _this.field,
            prop: _this.prop
          });
        });
      },
      selectedField: function selectedField() {
        var _this$selectedOption, _this$prop;
        var currentQuery = _yooessentials.SourceExtended.getQuery(this.node, this.field.name);
        var queryHasChanged = _yooessentials.SourceExtended.getSourceField((_this$selectedOption = this.selectedOption) == null ? void 0 : _this$selectedOption.query) !== _yooessentials.SourceExtended.getSourceField(currentQuery);
        if (queryHasChanged) {
          return null;
        }
        return (_this$prop = this.prop) == null ? void 0 : _this$prop.name;
      }
    },
    created: function created() {
      if (this.matchedOption) {
        this.setOption(this.matchedOption);
      }
    },
    methods: {
      setOption: function setOption(opt) {
        this.isSelected = true;
        this.selectedOption = opt;
      },
      resolve: function resolve(_ref) {
        var _this$selectedOption2;
        var value = _ref.value,
          _ref$enforce = _ref.enforce,
          enforce = _ref$enforce === void 0 ? false : _ref$enforce,
          payload = _objectWithoutPropertiesLoose(_ref, _excluded);
        var node = this.node;
        var field = this.field;
        var prop = _yooessentials.SourceExtended.getProp(node, field.name);
        if (!value || !((_this$selectedOption2 = this.selectedOption) != null && _this$selectedOption2.resolve)) {
          return;
        }
        if (!enforce && value === (prop == null ? void 0 : prop.name) && this.selectedOption === this.matchedOption) {
          return;
        }
        this.selectedOption.resolve(_extends({}, payload, {
          mappedTo: value,
          node: this.node,
          field: this.field
        }));
        this.$emit('resolve');
      }
    }
  };

  /* script */
  var __vue_script__$2 = script$2;

  /* template */
  var __vue_render__$2 = function __vue_render__() {
    var _vm = this;
    var _h = _vm.$createElement;
    var _c = _vm._self._c || _h;
    return !_vm.isSelected ? _c('DynamicDropdownOptions', {
      attrs: {
        "options": _vm.dynamicOptions,
        "selected": _vm.matchedOption
      },
      on: {
        "resolve": _vm.setOption
      }
    }) : _vm.selectedOption.component ? _c(_vm.selectedOption.component, {
      tag: "component",
      attrs: {
        "option": _vm.selectedOption
      },
      on: {
        "config": function config($event) {
          _vm.isSelected = false;
        },
        "resolve": _vm.resolve
      }
    }) : _c('DropdownList', {
      attrs: {
        "max-height": "large",
        "entries": _vm.selectedOption.fields(),
        "selected": _vm.selectedField
      },
      on: {
        "config": function config($event) {
          _vm.isSelected = false;
        },
        "resolve": function resolve(ref) {
          var value = ref.value;
          return _vm.resolve({
            value: value
          });
        }
      },
      scopedSlots: _vm._u([{
        key: "nav",
        fn: function fn() {
          return [_c('div', {
            staticClass: "uk-flex uk-flex-right uk-flex-middle"
          }, [_c('span', {
            staticClass: "uk-text-meta uk-margin-small-right"
          }, [_vm._v(_vm._s(_vm.selectedOption.title))]), _vm._v(" "), _c('a', {
            on: {
              "click": function click($event) {
                $event.preventDefault();
                _vm.isSelected = false;
              }
            }
          }, [_c('span', {
            staticClass: "uk-icon-link",
            attrs: {
              "uk-icon": "cog"
            }
          })])])];
        },
        proxy: true
      }])
    });
  };
  var __vue_staticRenderFns__$2 = [];

  /* style */
  var __vue_inject_styles__$2 = undefined;
  /* scoped */
  var __vue_scope_id__$2 = undefined;
  /* module identifier */
  var __vue_module_identifier__$2 = undefined;
  /* functional template */
  var __vue_is_functional_template__$2 = false;
  /* style inject */

  /* style inject SSR */

  /* style inject shadow dom */

  var __vue_component__$2 = /*#__PURE__*/normalizeComponent({
    render: __vue_render__$2,
    staticRenderFns: __vue_staticRenderFns__$2
  }, __vue_inject_styles__$2, __vue_script__$2, __vue_scope_id__$2, __vue_is_functional_template__$2, __vue_module_identifier__$2, false, undefined, undefined, undefined);

  //
  var $customizer = new Proxy({}, {
    get: function get(target, prop) {
      var _window$$yooessential, _window$$yooessential2;
      return (_window$$yooessential = window.$yooessentials) == null ? void 0 : (_window$$yooessential2 = _window$$yooessential.yoocustomizer) == null ? void 0 : _window$$yooessential2[prop];
    }
  });
  var SourcePanel = {
    "extends": _yooessentials.BuilderPanel,
    props: {
      Source: {
        type: Object,
        "default": function _default() {
          return _yooessentials.Source;
        }
      }
    }
  };
  var script$1 = {
    name: 'CustomQuery',
    components: {
      SourcePanel: SourcePanel
    },
    inject: ['Builder'],
    props: {
      values: {
        type: Object,
        "default": function _default() {
          return {};
        }
      }
    },
    data: function data() {
      return {
        node: {}
      };
    },
    computed: {
      query: function query() {
        var _this$node, _this$node$source;
        return (_this$node = this.node) == null ? void 0 : (_this$node$source = _this$node.source) == null ? void 0 : _this$node$source.query;
      },
      panel: function panel() {
        var _$customizer$panels;
        var panel = ($customizer == null ? void 0 : (_$customizer$panels = $customizer.panels) == null ? void 0 : _$customizer$panels['yooessentials-source-query-prop']) || {};
        delete panel.fields.name;
        return panel;
      },
      isDirty: function isDirty() {
        return JSON.stringify(this.values) !== JSON.stringify(this.node.source.query);
      }
    },
    created: function created() {
      this.node = {
        source: {
          query: _yooessentials.deepCopy(this.values)
        }
      };
    },
    methods: {
      isEmpty: uikitUtil.isEmpty
    }
  };

  /* script */
  var __vue_script__$1 = script$1;

  /* template */
  var __vue_render__$1 = function __vue_render__() {
    var _vm = this;
    var _h = _vm.$createElement;
    var _c = _vm._self._c || _h;
    return _c('div', [_c('div', {
      staticClass: "yo-dropdown-header"
    }, [_c('div', {
      staticClass: "uk-flex uk-flex-right uk-flex-middle uk-padding-small"
    }, [_c('div', {}, [_c('button', {
      staticClass: "uk-button uk-button-small uk-margin-small-right",
      on: {
        "click": function click($event) {
          $event.preventDefault();
          return _vm.$emit('cancel');
        }
      }
    }, [_c('span', {
      attrs: {
        "uk-icon": "arrow-left"
      }
    })]), _vm._v(" "), _c('button', {
      staticClass: "uk-button uk-button-primary uk-button-small",
      "class": {
        'uk-disabled': _vm.isEmpty(_vm.query) || !_vm.isDirty
      },
      attrs: {
        "disabled": _vm.isEmpty(_vm.query) || !_vm.isDirty
      },
      domProps: {
        "textContent": _vm._s(_vm.$t('Save'))
      },
      on: {
        "click": function click($event) {
          $event.preventDefault();
          return _vm.$emit('resolve', _vm.query);
        }
      }
    })])])]), _vm._v(" "), _c('div', {
      staticClass: "uk-height-max-large uk-overflow-auto"
    }, [_c('div', {
      staticClass: "uk-padding"
    }, [_c('SourcePanel', {
      attrs: {
        "node": _vm.node,
        "builder": _vm.Builder,
        "panel": _vm.panel,
        "values": {}
      }
    })], 1)])]);
  };
  var __vue_staticRenderFns__$1 = [];

  /* style */
  var __vue_inject_styles__$1 = undefined;
  /* scoped */
  var __vue_scope_id__$1 = undefined;
  /* module identifier */
  var __vue_module_identifier__$1 = undefined;
  /* functional template */
  var __vue_is_functional_template__$1 = false;
  /* style inject */

  /* style inject SSR */

  /* style inject shadow dom */

  var __vue_component__$1 = /*#__PURE__*/normalizeComponent({
    render: __vue_render__$1,
    staticRenderFns: __vue_staticRenderFns__$1
  }, __vue_inject_styles__$1, __vue_script__$1, __vue_scope_id__$1, __vue_is_functional_template__$1, __vue_module_identifier__$1, false, undefined, undefined, undefined);

  //
  var script = {
    name: 'DynamicCustomQuery',
    components: {
      DropdownList: _yooessentials.DropdownList,
      DynamicCustomQuerySettings: __vue_component__$1
    },
    inject: ['$node', '$field'],
    props: {
      option: {
        type: Object,
        required: true
      }
    },
    data: function data() {
      return {
        view: '',
        // fields|query
        query: {}
      };
    },
    computed: {
      fields: function fields() {
        return _yooessentials.SourceExtended.getFieldsList(this.query);
      },
      selectedField: function selectedField() {
        var _SourceExtended$getPr;
        return (_SourceExtended$getPr = _yooessentials.SourceExtended.getProp(this.$node, this.$field.name)) == null ? void 0 : _SourceExtended$getPr.name;
      }
    },
    created: function created() {
      var prop = _yooessentials.SourceExtended.getProp(this.$node, this.$field.name);
      var origin = _yooessentials.SourceExtended.getQueryOrigin(this.$node, this.$field.name);
      if (origin === 'query') {
        this.setQuery(prop.query);
      }
    },
    methods: {
      setQuery: function setQuery(query) {
        this.query = query;
        this.view = 'fields';
      }
    }
  };

  /* script */
  var __vue_script__ = script;

  /* template */
  var __vue_render__ = function __vue_render__() {
    var _vm = this;
    var _h = _vm.$createElement;
    var _c = _vm._self._c || _h;
    return _vm.view === '' ? _c('DynamicCustomQuerySettings', {
      attrs: {
        "values": _vm.query
      },
      on: {
        "cancel": function cancel($event) {
          return _vm.$emit('config');
        },
        "resolve": _vm.setQuery
      }
    }) : _c('DropdownList', {
      attrs: {
        "entries": _vm.fields,
        "label": _vm.$t('Custom'),
        "selected": _vm.selectedField
      },
      on: {
        "resolve": function resolve(ref) {
          var value = ref.value;
          return _vm.$emit('resolve', {
            value: value,
            query: _vm.query,
            enforce: true
          });
        }
      },
      scopedSlots: _vm._u([{
        key: "nav",
        fn: function fn() {
          return [_c('div', {
            staticClass: "uk-flex uk-flex-right uk-flex-middle"
          }, [_c('span', {
            staticClass: "uk-text-meta uk-margin-small-right"
          }, [_vm._v(_vm._s(_vm.$t('Custom')))]), _vm._v(" "), _c('a', {
            on: {
              "click": function click($event) {
                $event.preventDefault();
                _vm.view = '';
              }
            }
          }, [_c('span', {
            staticClass: "uk-icon-link",
            attrs: {
              "uk-icon": "cog"
            }
          })])])];
        },
        proxy: true
      }])
    });
  };
  var __vue_staticRenderFns__ = [];

  /* style */
  var __vue_inject_styles__ = undefined;
  /* scoped */
  var __vue_scope_id__ = undefined;
  /* module identifier */
  var __vue_module_identifier__ = undefined;
  /* functional template */
  var __vue_is_functional_template__ = false;
  /* style inject */

  /* style inject SSR */

  /* style inject shadow dom */

  var __vue_component__ = /*#__PURE__*/normalizeComponent({
    render: __vue_render__,
    staticRenderFns: __vue_staticRenderFns__
  }, __vue_inject_styles__, __vue_script__, __vue_scope_id__, __vue_is_functional_template__, __vue_module_identifier__, false, undefined, undefined, undefined);

  var DynamicBuilderPanel = {
    props: ['node'],
    computed: {
      evaluateValues: function evaluateValues() {
        var _this$node$source, _this$node, _this$node$source_ext, _this$node2, _this$node2$source, _this$node3, _this$node3$source_ex;
        return (_this$node$source = this.node.source) != null && _this$node$source.props || (_this$node = this.node) != null && (_this$node$source_ext = _this$node.source_extended) != null && _this$node$source_ext.props ? _extends({}, this.values, ((_this$node2 = this.node) == null ? void 0 : (_this$node2$source = _this$node2.source) == null ? void 0 : _this$node2$source.props) || {}, ((_this$node3 = this.node) == null ? void 0 : (_this$node3$source_ex = _this$node3.source_extended) == null ? void 0 : _this$node3$source_ex.props) || {}) : this.values;
      }
    },
    methods: {
      prepare: function prepare(config) {
        var _this = this;
        return _yooessentials.FieldsPanel.methods.prepare.call(this, config).map(function (field) {
          _this.$trigger('yooessentialsBuilderPrepareField', {
            node: _this.node,
            field: field
          });
          return field;
        });
      }
    }
  };

  /**
   * A specialized version of `_.map` for arrays without support for iteratee
   * shorthands.
   *
   * @private
   * @param {Array} [array] The array to iterate over.
   * @param {Function} iteratee The function invoked per iteration.
   * @returns {Array} Returns the new mapped array.
   */
  function arrayMap(array, iteratee) {
    var index = -1,
      length = array == null ? 0 : array.length,
      result = Array(length);
    while (++index < length) {
      result[index] = iteratee(array[index], index, array);
    }
    return result;
  }

  /**
   * Removes all key-value entries from the list cache.
   *
   * @private
   * @name clear
   * @memberOf ListCache
   */
  function listCacheClear() {
    this.__data__ = [];
    this.size = 0;
  }

  /**
   * Performs a
   * [`SameValueZero`](http://ecma-international.org/ecma-262/7.0/#sec-samevaluezero)
   * comparison between two values to determine if they are equivalent.
   *
   * @static
   * @memberOf _
   * @since 4.0.0
   * @category Lang
   * @param {*} value The value to compare.
   * @param {*} other The other value to compare.
   * @returns {boolean} Returns `true` if the values are equivalent, else `false`.
   * @example
   *
   * var object = { 'a': 1 };
   * var other = { 'a': 1 };
   *
   * _.eq(object, object);
   * // => true
   *
   * _.eq(object, other);
   * // => false
   *
   * _.eq('a', 'a');
   * // => true
   *
   * _.eq('a', Object('a'));
   * // => false
   *
   * _.eq(NaN, NaN);
   * // => true
   */
  function eq(value, other) {
    return value === other || value !== value && other !== other;
  }

  /**
   * Gets the index at which the `key` is found in `array` of key-value pairs.
   *
   * @private
   * @param {Array} array The array to inspect.
   * @param {*} key The key to search for.
   * @returns {number} Returns the index of the matched value, else `-1`.
   */
  function assocIndexOf(array, key) {
    var length = array.length;
    while (length--) {
      if (eq(array[length][0], key)) {
        return length;
      }
    }
    return -1;
  }

  /** Used for built-in method references. */
  var arrayProto = Array.prototype;

  /** Built-in value references. */
  var splice = arrayProto.splice;

  /**
   * Removes `key` and its value from the list cache.
   *
   * @private
   * @name delete
   * @memberOf ListCache
   * @param {string} key The key of the value to remove.
   * @returns {boolean} Returns `true` if the entry was removed, else `false`.
   */
  function listCacheDelete(key) {
    var data = this.__data__,
      index = assocIndexOf(data, key);
    if (index < 0) {
      return false;
    }
    var lastIndex = data.length - 1;
    if (index == lastIndex) {
      data.pop();
    } else {
      splice.call(data, index, 1);
    }
    --this.size;
    return true;
  }

  /**
   * Gets the list cache value for `key`.
   *
   * @private
   * @name get
   * @memberOf ListCache
   * @param {string} key The key of the value to get.
   * @returns {*} Returns the entry value.
   */
  function listCacheGet(key) {
    var data = this.__data__,
      index = assocIndexOf(data, key);
    return index < 0 ? undefined : data[index][1];
  }

  /**
   * Checks if a list cache value for `key` exists.
   *
   * @private
   * @name has
   * @memberOf ListCache
   * @param {string} key The key of the entry to check.
   * @returns {boolean} Returns `true` if an entry for `key` exists, else `false`.
   */
  function listCacheHas(key) {
    return assocIndexOf(this.__data__, key) > -1;
  }

  /**
   * Sets the list cache `key` to `value`.
   *
   * @private
   * @name set
   * @memberOf ListCache
   * @param {string} key The key of the value to set.
   * @param {*} value The value to set.
   * @returns {Object} Returns the list cache instance.
   */
  function listCacheSet(key, value) {
    var data = this.__data__,
      index = assocIndexOf(data, key);
    if (index < 0) {
      ++this.size;
      data.push([key, value]);
    } else {
      data[index][1] = value;
    }
    return this;
  }

  /**
   * Creates an list cache object.
   *
   * @private
   * @constructor
   * @param {Array} [entries] The key-value pairs to cache.
   */
  function ListCache(entries) {
    var index = -1,
      length = entries == null ? 0 : entries.length;
    this.clear();
    while (++index < length) {
      var entry = entries[index];
      this.set(entry[0], entry[1]);
    }
  }

  // Add methods to `ListCache`.
  ListCache.prototype.clear = listCacheClear;
  ListCache.prototype['delete'] = listCacheDelete;
  ListCache.prototype.get = listCacheGet;
  ListCache.prototype.has = listCacheHas;
  ListCache.prototype.set = listCacheSet;

  /**
   * Removes all key-value entries from the stack.
   *
   * @private
   * @name clear
   * @memberOf Stack
   */
  function stackClear() {
    this.__data__ = new ListCache();
    this.size = 0;
  }

  /**
   * Removes `key` and its value from the stack.
   *
   * @private
   * @name delete
   * @memberOf Stack
   * @param {string} key The key of the value to remove.
   * @returns {boolean} Returns `true` if the entry was removed, else `false`.
   */
  function stackDelete(key) {
    var data = this.__data__,
      result = data['delete'](key);
    this.size = data.size;
    return result;
  }

  /**
   * Gets the stack value for `key`.
   *
   * @private
   * @name get
   * @memberOf Stack
   * @param {string} key The key of the value to get.
   * @returns {*} Returns the entry value.
   */
  function stackGet(key) {
    return this.__data__.get(key);
  }

  /**
   * Checks if a stack value for `key` exists.
   *
   * @private
   * @name has
   * @memberOf Stack
   * @param {string} key The key of the entry to check.
   * @returns {boolean} Returns `true` if an entry for `key` exists, else `false`.
   */
  function stackHas(key) {
    return this.__data__.has(key);
  }

  /** Detect free variable `global` from Node.js. */
  var freeGlobal = typeof global == 'object' && global && global.Object === Object && global;
  var freeGlobal$1 = freeGlobal;

  /** Detect free variable `self`. */
  var freeSelf = typeof self == 'object' && self && self.Object === Object && self;

  /** Used as a reference to the global object. */
  var root = freeGlobal$1 || freeSelf || Function('return this')();
  var root$1 = root;

  /** Built-in value references. */
  var Symbol$1 = root$1.Symbol;
  var Symbol$2 = Symbol$1;

  /** Used for built-in method references. */
  var objectProto$c = Object.prototype;

  /** Used to check objects for own properties. */
  var hasOwnProperty$9 = objectProto$c.hasOwnProperty;

  /**
   * Used to resolve the
   * [`toStringTag`](http://ecma-international.org/ecma-262/7.0/#sec-object.prototype.tostring)
   * of values.
   */
  var nativeObjectToString$1 = objectProto$c.toString;

  /** Built-in value references. */
  var symToStringTag$1 = Symbol$2 ? Symbol$2.toStringTag : undefined;

  /**
   * A specialized version of `baseGetTag` which ignores `Symbol.toStringTag` values.
   *
   * @private
   * @param {*} value The value to query.
   * @returns {string} Returns the raw `toStringTag`.
   */
  function getRawTag(value) {
    var isOwn = hasOwnProperty$9.call(value, symToStringTag$1),
      tag = value[symToStringTag$1];
    try {
      value[symToStringTag$1] = undefined;
      var unmasked = true;
    } catch (e) {}
    var result = nativeObjectToString$1.call(value);
    if (unmasked) {
      if (isOwn) {
        value[symToStringTag$1] = tag;
      } else {
        delete value[symToStringTag$1];
      }
    }
    return result;
  }

  /** Used for built-in method references. */
  var objectProto$b = Object.prototype;

  /**
   * Used to resolve the
   * [`toStringTag`](http://ecma-international.org/ecma-262/7.0/#sec-object.prototype.tostring)
   * of values.
   */
  var nativeObjectToString = objectProto$b.toString;

  /**
   * Converts `value` to a string using `Object.prototype.toString`.
   *
   * @private
   * @param {*} value The value to convert.
   * @returns {string} Returns the converted string.
   */
  function objectToString(value) {
    return nativeObjectToString.call(value);
  }

  /** `Object#toString` result references. */
  var nullTag = '[object Null]',
    undefinedTag = '[object Undefined]';

  /** Built-in value references. */
  var symToStringTag = Symbol$2 ? Symbol$2.toStringTag : undefined;

  /**
   * The base implementation of `getTag` without fallbacks for buggy environments.
   *
   * @private
   * @param {*} value The value to query.
   * @returns {string} Returns the `toStringTag`.
   */
  function baseGetTag(value) {
    if (value == null) {
      return value === undefined ? undefinedTag : nullTag;
    }
    return symToStringTag && symToStringTag in Object(value) ? getRawTag(value) : objectToString(value);
  }

  /**
   * Checks if `value` is the
   * [language type](http://www.ecma-international.org/ecma-262/7.0/#sec-ecmascript-language-types)
   * of `Object`. (e.g. arrays, functions, objects, regexes, `new Number(0)`, and `new String('')`)
   *
   * @static
   * @memberOf _
   * @since 0.1.0
   * @category Lang
   * @param {*} value The value to check.
   * @returns {boolean} Returns `true` if `value` is an object, else `false`.
   * @example
   *
   * _.isObject({});
   * // => true
   *
   * _.isObject([1, 2, 3]);
   * // => true
   *
   * _.isObject(_.noop);
   * // => true
   *
   * _.isObject(null);
   * // => false
   */
  function isObject(value) {
    var type = typeof value;
    return value != null && (type == 'object' || type == 'function');
  }

  /** `Object#toString` result references. */
  var asyncTag = '[object AsyncFunction]',
    funcTag$1 = '[object Function]',
    genTag = '[object GeneratorFunction]',
    proxyTag = '[object Proxy]';

  /**
   * Checks if `value` is classified as a `Function` object.
   *
   * @static
   * @memberOf _
   * @since 0.1.0
   * @category Lang
   * @param {*} value The value to check.
   * @returns {boolean} Returns `true` if `value` is a function, else `false`.
   * @example
   *
   * _.isFunction(_);
   * // => true
   *
   * _.isFunction(/abc/);
   * // => false
   */
  function isFunction(value) {
    if (!isObject(value)) {
      return false;
    }
    // The use of `Object#toString` avoids issues with the `typeof` operator
    // in Safari 9 which returns 'object' for typed arrays and other constructors.
    var tag = baseGetTag(value);
    return tag == funcTag$1 || tag == genTag || tag == asyncTag || tag == proxyTag;
  }

  /** Used to detect overreaching core-js shims. */
  var coreJsData = root$1['__core-js_shared__'];
  var coreJsData$1 = coreJsData;

  /** Used to detect methods masquerading as native. */
  var maskSrcKey = function () {
    var uid = /[^.]+$/.exec(coreJsData$1 && coreJsData$1.keys && coreJsData$1.keys.IE_PROTO || '');
    return uid ? 'Symbol(src)_1.' + uid : '';
  }();

  /**
   * Checks if `func` has its source masked.
   *
   * @private
   * @param {Function} func The function to check.
   * @returns {boolean} Returns `true` if `func` is masked, else `false`.
   */
  function isMasked(func) {
    return !!maskSrcKey && maskSrcKey in func;
  }

  /** Used for built-in method references. */
  var funcProto$1 = Function.prototype;

  /** Used to resolve the decompiled source of functions. */
  var funcToString$1 = funcProto$1.toString;

  /**
   * Converts `func` to its source code.
   *
   * @private
   * @param {Function} func The function to convert.
   * @returns {string} Returns the source code.
   */
  function toSource(func) {
    if (func != null) {
      try {
        return funcToString$1.call(func);
      } catch (e) {}
      try {
        return func + '';
      } catch (e) {}
    }
    return '';
  }

  /**
   * Used to match `RegExp`
   * [syntax characters](http://ecma-international.org/ecma-262/7.0/#sec-patterns).
   */
  var reRegExpChar = /[\\^$.*+?()[\]{}|]/g;

  /** Used to detect host constructors (Safari). */
  var reIsHostCtor = /^\[object .+?Constructor\]$/;

  /** Used for built-in method references. */
  var funcProto = Function.prototype,
    objectProto$a = Object.prototype;

  /** Used to resolve the decompiled source of functions. */
  var funcToString = funcProto.toString;

  /** Used to check objects for own properties. */
  var hasOwnProperty$8 = objectProto$a.hasOwnProperty;

  /** Used to detect if a method is native. */
  var reIsNative = RegExp('^' + funcToString.call(hasOwnProperty$8).replace(reRegExpChar, '\\$&').replace(/hasOwnProperty|(function).*?(?=\\\()| for .+?(?=\\\])/g, '$1.*?') + '$');

  /**
   * The base implementation of `_.isNative` without bad shim checks.
   *
   * @private
   * @param {*} value The value to check.
   * @returns {boolean} Returns `true` if `value` is a native function,
   *  else `false`.
   */
  function baseIsNative(value) {
    if (!isObject(value) || isMasked(value)) {
      return false;
    }
    var pattern = isFunction(value) ? reIsNative : reIsHostCtor;
    return pattern.test(toSource(value));
  }

  /**
   * Gets the value at `key` of `object`.
   *
   * @private
   * @param {Object} [object] The object to query.
   * @param {string} key The key of the property to get.
   * @returns {*} Returns the property value.
   */
  function getValue(object, key) {
    return object == null ? undefined : object[key];
  }

  /**
   * Gets the native function at `key` of `object`.
   *
   * @private
   * @param {Object} object The object to query.
   * @param {string} key The key of the method to get.
   * @returns {*} Returns the function if it's native, else `undefined`.
   */
  function getNative(object, key) {
    var value = getValue(object, key);
    return baseIsNative(value) ? value : undefined;
  }

  /* Built-in method references that are verified to be native. */
  var Map = getNative(root$1, 'Map');
  var Map$1 = Map;

  /* Built-in method references that are verified to be native. */
  var nativeCreate = getNative(Object, 'create');
  var nativeCreate$1 = nativeCreate;

  /**
   * Removes all key-value entries from the hash.
   *
   * @private
   * @name clear
   * @memberOf Hash
   */
  function hashClear() {
    this.__data__ = nativeCreate$1 ? nativeCreate$1(null) : {};
    this.size = 0;
  }

  /**
   * Removes `key` and its value from the hash.
   *
   * @private
   * @name delete
   * @memberOf Hash
   * @param {Object} hash The hash to modify.
   * @param {string} key The key of the value to remove.
   * @returns {boolean} Returns `true` if the entry was removed, else `false`.
   */
  function hashDelete(key) {
    var result = this.has(key) && delete this.__data__[key];
    this.size -= result ? 1 : 0;
    return result;
  }

  /** Used to stand-in for `undefined` hash values. */
  var HASH_UNDEFINED$2 = '__lodash_hash_undefined__';

  /** Used for built-in method references. */
  var objectProto$9 = Object.prototype;

  /** Used to check objects for own properties. */
  var hasOwnProperty$7 = objectProto$9.hasOwnProperty;

  /**
   * Gets the hash value for `key`.
   *
   * @private
   * @name get
   * @memberOf Hash
   * @param {string} key The key of the value to get.
   * @returns {*} Returns the entry value.
   */
  function hashGet(key) {
    var data = this.__data__;
    if (nativeCreate$1) {
      var result = data[key];
      return result === HASH_UNDEFINED$2 ? undefined : result;
    }
    return hasOwnProperty$7.call(data, key) ? data[key] : undefined;
  }

  /** Used for built-in method references. */
  var objectProto$8 = Object.prototype;

  /** Used to check objects for own properties. */
  var hasOwnProperty$6 = objectProto$8.hasOwnProperty;

  /**
   * Checks if a hash value for `key` exists.
   *
   * @private
   * @name has
   * @memberOf Hash
   * @param {string} key The key of the entry to check.
   * @returns {boolean} Returns `true` if an entry for `key` exists, else `false`.
   */
  function hashHas(key) {
    var data = this.__data__;
    return nativeCreate$1 ? data[key] !== undefined : hasOwnProperty$6.call(data, key);
  }

  /** Used to stand-in for `undefined` hash values. */
  var HASH_UNDEFINED$1 = '__lodash_hash_undefined__';

  /**
   * Sets the hash `key` to `value`.
   *
   * @private
   * @name set
   * @memberOf Hash
   * @param {string} key The key of the value to set.
   * @param {*} value The value to set.
   * @returns {Object} Returns the hash instance.
   */
  function hashSet(key, value) {
    var data = this.__data__;
    this.size += this.has(key) ? 0 : 1;
    data[key] = nativeCreate$1 && value === undefined ? HASH_UNDEFINED$1 : value;
    return this;
  }

  /**
   * Creates a hash object.
   *
   * @private
   * @constructor
   * @param {Array} [entries] The key-value pairs to cache.
   */
  function Hash(entries) {
    var index = -1,
      length = entries == null ? 0 : entries.length;
    this.clear();
    while (++index < length) {
      var entry = entries[index];
      this.set(entry[0], entry[1]);
    }
  }

  // Add methods to `Hash`.
  Hash.prototype.clear = hashClear;
  Hash.prototype['delete'] = hashDelete;
  Hash.prototype.get = hashGet;
  Hash.prototype.has = hashHas;
  Hash.prototype.set = hashSet;

  /**
   * Removes all key-value entries from the map.
   *
   * @private
   * @name clear
   * @memberOf MapCache
   */
  function mapCacheClear() {
    this.size = 0;
    this.__data__ = {
      'hash': new Hash(),
      'map': new (Map$1 || ListCache)(),
      'string': new Hash()
    };
  }

  /**
   * Checks if `value` is suitable for use as unique object key.
   *
   * @private
   * @param {*} value The value to check.
   * @returns {boolean} Returns `true` if `value` is suitable, else `false`.
   */
  function isKeyable(value) {
    var type = typeof value;
    return type == 'string' || type == 'number' || type == 'symbol' || type == 'boolean' ? value !== '__proto__' : value === null;
  }

  /**
   * Gets the data for `map`.
   *
   * @private
   * @param {Object} map The map to query.
   * @param {string} key The reference key.
   * @returns {*} Returns the map data.
   */
  function getMapData(map, key) {
    var data = map.__data__;
    return isKeyable(key) ? data[typeof key == 'string' ? 'string' : 'hash'] : data.map;
  }

  /**
   * Removes `key` and its value from the map.
   *
   * @private
   * @name delete
   * @memberOf MapCache
   * @param {string} key The key of the value to remove.
   * @returns {boolean} Returns `true` if the entry was removed, else `false`.
   */
  function mapCacheDelete(key) {
    var result = getMapData(this, key)['delete'](key);
    this.size -= result ? 1 : 0;
    return result;
  }

  /**
   * Gets the map value for `key`.
   *
   * @private
   * @name get
   * @memberOf MapCache
   * @param {string} key The key of the value to get.
   * @returns {*} Returns the entry value.
   */
  function mapCacheGet(key) {
    return getMapData(this, key).get(key);
  }

  /**
   * Checks if a map value for `key` exists.
   *
   * @private
   * @name has
   * @memberOf MapCache
   * @param {string} key The key of the entry to check.
   * @returns {boolean} Returns `true` if an entry for `key` exists, else `false`.
   */
  function mapCacheHas(key) {
    return getMapData(this, key).has(key);
  }

  /**
   * Sets the map `key` to `value`.
   *
   * @private
   * @name set
   * @memberOf MapCache
   * @param {string} key The key of the value to set.
   * @param {*} value The value to set.
   * @returns {Object} Returns the map cache instance.
   */
  function mapCacheSet(key, value) {
    var data = getMapData(this, key),
      size = data.size;
    data.set(key, value);
    this.size += data.size == size ? 0 : 1;
    return this;
  }

  /**
   * Creates a map cache object to store key-value pairs.
   *
   * @private
   * @constructor
   * @param {Array} [entries] The key-value pairs to cache.
   */
  function MapCache(entries) {
    var index = -1,
      length = entries == null ? 0 : entries.length;
    this.clear();
    while (++index < length) {
      var entry = entries[index];
      this.set(entry[0], entry[1]);
    }
  }

  // Add methods to `MapCache`.
  MapCache.prototype.clear = mapCacheClear;
  MapCache.prototype['delete'] = mapCacheDelete;
  MapCache.prototype.get = mapCacheGet;
  MapCache.prototype.has = mapCacheHas;
  MapCache.prototype.set = mapCacheSet;

  /** Used as the size to enable large array optimizations. */
  var LARGE_ARRAY_SIZE = 200;

  /**
   * Sets the stack `key` to `value`.
   *
   * @private
   * @name set
   * @memberOf Stack
   * @param {string} key The key of the value to set.
   * @param {*} value The value to set.
   * @returns {Object} Returns the stack cache instance.
   */
  function stackSet(key, value) {
    var data = this.__data__;
    if (data instanceof ListCache) {
      var pairs = data.__data__;
      if (!Map$1 || pairs.length < LARGE_ARRAY_SIZE - 1) {
        pairs.push([key, value]);
        this.size = ++data.size;
        return this;
      }
      data = this.__data__ = new MapCache(pairs);
    }
    data.set(key, value);
    this.size = data.size;
    return this;
  }

  /**
   * Creates a stack cache object to store key-value pairs.
   *
   * @private
   * @constructor
   * @param {Array} [entries] The key-value pairs to cache.
   */
  function Stack(entries) {
    var data = this.__data__ = new ListCache(entries);
    this.size = data.size;
  }

  // Add methods to `Stack`.
  Stack.prototype.clear = stackClear;
  Stack.prototype['delete'] = stackDelete;
  Stack.prototype.get = stackGet;
  Stack.prototype.has = stackHas;
  Stack.prototype.set = stackSet;

  /** Used to stand-in for `undefined` hash values. */
  var HASH_UNDEFINED = '__lodash_hash_undefined__';

  /**
   * Adds `value` to the array cache.
   *
   * @private
   * @name add
   * @memberOf SetCache
   * @alias push
   * @param {*} value The value to cache.
   * @returns {Object} Returns the cache instance.
   */
  function setCacheAdd(value) {
    this.__data__.set(value, HASH_UNDEFINED);
    return this;
  }

  /**
   * Checks if `value` is in the array cache.
   *
   * @private
   * @name has
   * @memberOf SetCache
   * @param {*} value The value to search for.
   * @returns {number} Returns `true` if `value` is found, else `false`.
   */
  function setCacheHas(value) {
    return this.__data__.has(value);
  }

  /**
   *
   * Creates an array cache object to store unique values.
   *
   * @private
   * @constructor
   * @param {Array} [values] The values to cache.
   */
  function SetCache(values) {
    var index = -1,
      length = values == null ? 0 : values.length;
    this.__data__ = new MapCache();
    while (++index < length) {
      this.add(values[index]);
    }
  }

  // Add methods to `SetCache`.
  SetCache.prototype.add = SetCache.prototype.push = setCacheAdd;
  SetCache.prototype.has = setCacheHas;

  /**
   * A specialized version of `_.some` for arrays without support for iteratee
   * shorthands.
   *
   * @private
   * @param {Array} [array] The array to iterate over.
   * @param {Function} predicate The function invoked per iteration.
   * @returns {boolean} Returns `true` if any element passes the predicate check,
   *  else `false`.
   */
  function arraySome(array, predicate) {
    var index = -1,
      length = array == null ? 0 : array.length;
    while (++index < length) {
      if (predicate(array[index], index, array)) {
        return true;
      }
    }
    return false;
  }

  /**
   * Checks if a `cache` value for `key` exists.
   *
   * @private
   * @param {Object} cache The cache to query.
   * @param {string} key The key of the entry to check.
   * @returns {boolean} Returns `true` if an entry for `key` exists, else `false`.
   */
  function cacheHas(cache, key) {
    return cache.has(key);
  }

  /** Used to compose bitmasks for value comparisons. */
  var COMPARE_PARTIAL_FLAG$5 = 1,
    COMPARE_UNORDERED_FLAG$3 = 2;

  /**
   * A specialized version of `baseIsEqualDeep` for arrays with support for
   * partial deep comparisons.
   *
   * @private
   * @param {Array} array The array to compare.
   * @param {Array} other The other array to compare.
   * @param {number} bitmask The bitmask flags. See `baseIsEqual` for more details.
   * @param {Function} customizer The function to customize comparisons.
   * @param {Function} equalFunc The function to determine equivalents of values.
   * @param {Object} stack Tracks traversed `array` and `other` objects.
   * @returns {boolean} Returns `true` if the arrays are equivalent, else `false`.
   */
  function equalArrays(array, other, bitmask, customizer, equalFunc, stack) {
    var isPartial = bitmask & COMPARE_PARTIAL_FLAG$5,
      arrLength = array.length,
      othLength = other.length;
    if (arrLength != othLength && !(isPartial && othLength > arrLength)) {
      return false;
    }
    // Check that cyclic values are equal.
    var arrStacked = stack.get(array);
    var othStacked = stack.get(other);
    if (arrStacked && othStacked) {
      return arrStacked == other && othStacked == array;
    }
    var index = -1,
      result = true,
      seen = bitmask & COMPARE_UNORDERED_FLAG$3 ? new SetCache() : undefined;
    stack.set(array, other);
    stack.set(other, array);

    // Ignore non-index properties.
    while (++index < arrLength) {
      var arrValue = array[index],
        othValue = other[index];
      if (customizer) {
        var compared = isPartial ? customizer(othValue, arrValue, index, other, array, stack) : customizer(arrValue, othValue, index, array, other, stack);
      }
      if (compared !== undefined) {
        if (compared) {
          continue;
        }
        result = false;
        break;
      }
      // Recursively compare arrays (susceptible to call stack limits).
      if (seen) {
        if (!arraySome(other, function (othValue, othIndex) {
          if (!cacheHas(seen, othIndex) && (arrValue === othValue || equalFunc(arrValue, othValue, bitmask, customizer, stack))) {
            return seen.push(othIndex);
          }
        })) {
          result = false;
          break;
        }
      } else if (!(arrValue === othValue || equalFunc(arrValue, othValue, bitmask, customizer, stack))) {
        result = false;
        break;
      }
    }
    stack['delete'](array);
    stack['delete'](other);
    return result;
  }

  /** Built-in value references. */
  var Uint8Array = root$1.Uint8Array;
  var Uint8Array$1 = Uint8Array;

  /**
   * Converts `map` to its key-value pairs.
   *
   * @private
   * @param {Object} map The map to convert.
   * @returns {Array} Returns the key-value pairs.
   */
  function mapToArray(map) {
    var index = -1,
      result = Array(map.size);
    map.forEach(function (value, key) {
      result[++index] = [key, value];
    });
    return result;
  }

  /**
   * Converts `set` to an array of its values.
   *
   * @private
   * @param {Object} set The set to convert.
   * @returns {Array} Returns the values.
   */
  function setToArray(set) {
    var index = -1,
      result = Array(set.size);
    set.forEach(function (value) {
      result[++index] = value;
    });
    return result;
  }

  /** Used to compose bitmasks for value comparisons. */
  var COMPARE_PARTIAL_FLAG$4 = 1,
    COMPARE_UNORDERED_FLAG$2 = 2;

  /** `Object#toString` result references. */
  var boolTag$1 = '[object Boolean]',
    dateTag$1 = '[object Date]',
    errorTag$1 = '[object Error]',
    mapTag$2 = '[object Map]',
    numberTag$1 = '[object Number]',
    regexpTag$1 = '[object RegExp]',
    setTag$2 = '[object Set]',
    stringTag$1 = '[object String]',
    symbolTag$1 = '[object Symbol]';
  var arrayBufferTag$1 = '[object ArrayBuffer]',
    dataViewTag$2 = '[object DataView]';

  /** Used to convert symbols to primitives and strings. */
  var symbolProto$1 = Symbol$2 ? Symbol$2.prototype : undefined,
    symbolValueOf = symbolProto$1 ? symbolProto$1.valueOf : undefined;

  /**
   * A specialized version of `baseIsEqualDeep` for comparing objects of
   * the same `toStringTag`.
   *
   * **Note:** This function only supports comparing values with tags of
   * `Boolean`, `Date`, `Error`, `Number`, `RegExp`, or `String`.
   *
   * @private
   * @param {Object} object The object to compare.
   * @param {Object} other The other object to compare.
   * @param {string} tag The `toStringTag` of the objects to compare.
   * @param {number} bitmask The bitmask flags. See `baseIsEqual` for more details.
   * @param {Function} customizer The function to customize comparisons.
   * @param {Function} equalFunc The function to determine equivalents of values.
   * @param {Object} stack Tracks traversed `object` and `other` objects.
   * @returns {boolean} Returns `true` if the objects are equivalent, else `false`.
   */
  function equalByTag(object, other, tag, bitmask, customizer, equalFunc, stack) {
    switch (tag) {
      case dataViewTag$2:
        if (object.byteLength != other.byteLength || object.byteOffset != other.byteOffset) {
          return false;
        }
        object = object.buffer;
        other = other.buffer;
      case arrayBufferTag$1:
        if (object.byteLength != other.byteLength || !equalFunc(new Uint8Array$1(object), new Uint8Array$1(other))) {
          return false;
        }
        return true;
      case boolTag$1:
      case dateTag$1:
      case numberTag$1:
        // Coerce booleans to `1` or `0` and dates to milliseconds.
        // Invalid dates are coerced to `NaN`.
        return eq(+object, +other);
      case errorTag$1:
        return object.name == other.name && object.message == other.message;
      case regexpTag$1:
      case stringTag$1:
        // Coerce regexes to strings and treat strings, primitives and objects,
        // as equal. See http://www.ecma-international.org/ecma-262/7.0/#sec-regexp.prototype.tostring
        // for more details.
        return object == other + '';
      case mapTag$2:
        var convert = mapToArray;
      case setTag$2:
        var isPartial = bitmask & COMPARE_PARTIAL_FLAG$4;
        convert || (convert = setToArray);
        if (object.size != other.size && !isPartial) {
          return false;
        }
        // Assume cyclic values are equal.
        var stacked = stack.get(object);
        if (stacked) {
          return stacked == other;
        }
        bitmask |= COMPARE_UNORDERED_FLAG$2;

        // Recursively compare objects (susceptible to call stack limits).
        stack.set(object, other);
        var result = equalArrays(convert(object), convert(other), bitmask, customizer, equalFunc, stack);
        stack['delete'](object);
        return result;
      case symbolTag$1:
        if (symbolValueOf) {
          return symbolValueOf.call(object) == symbolValueOf.call(other);
        }
    }
    return false;
  }

  /**
   * Appends the elements of `values` to `array`.
   *
   * @private
   * @param {Array} array The array to modify.
   * @param {Array} values The values to append.
   * @returns {Array} Returns `array`.
   */
  function arrayPush(array, values) {
    var index = -1,
      length = values.length,
      offset = array.length;
    while (++index < length) {
      array[offset + index] = values[index];
    }
    return array;
  }

  /**
   * Checks if `value` is classified as an `Array` object.
   *
   * @static
   * @memberOf _
   * @since 0.1.0
   * @category Lang
   * @param {*} value The value to check.
   * @returns {boolean} Returns `true` if `value` is an array, else `false`.
   * @example
   *
   * _.isArray([1, 2, 3]);
   * // => true
   *
   * _.isArray(document.body.children);
   * // => false
   *
   * _.isArray('abc');
   * // => false
   *
   * _.isArray(_.noop);
   * // => false
   */
  var isArray = Array.isArray;
  var isArray$1 = isArray;

  /**
   * The base implementation of `getAllKeys` and `getAllKeysIn` which uses
   * `keysFunc` and `symbolsFunc` to get the enumerable property names and
   * symbols of `object`.
   *
   * @private
   * @param {Object} object The object to query.
   * @param {Function} keysFunc The function to get the keys of `object`.
   * @param {Function} symbolsFunc The function to get the symbols of `object`.
   * @returns {Array} Returns the array of property names and symbols.
   */
  function baseGetAllKeys(object, keysFunc, symbolsFunc) {
    var result = keysFunc(object);
    return isArray$1(object) ? result : arrayPush(result, symbolsFunc(object));
  }

  /**
   * A specialized version of `_.filter` for arrays without support for
   * iteratee shorthands.
   *
   * @private
   * @param {Array} [array] The array to iterate over.
   * @param {Function} predicate The function invoked per iteration.
   * @returns {Array} Returns the new filtered array.
   */
  function arrayFilter(array, predicate) {
    var index = -1,
      length = array == null ? 0 : array.length,
      resIndex = 0,
      result = [];
    while (++index < length) {
      var value = array[index];
      if (predicate(value, index, array)) {
        result[resIndex++] = value;
      }
    }
    return result;
  }

  /**
   * This method returns a new empty array.
   *
   * @static
   * @memberOf _
   * @since 4.13.0
   * @category Util
   * @returns {Array} Returns the new empty array.
   * @example
   *
   * var arrays = _.times(2, _.stubArray);
   *
   * console.log(arrays);
   * // => [[], []]
   *
   * console.log(arrays[0] === arrays[1]);
   * // => false
   */
  function stubArray() {
    return [];
  }

  /** Used for built-in method references. */
  var objectProto$7 = Object.prototype;

  /** Built-in value references. */
  var propertyIsEnumerable$1 = objectProto$7.propertyIsEnumerable;

  /* Built-in method references for those with the same name as other `lodash` methods. */
  var nativeGetSymbols = Object.getOwnPropertySymbols;

  /**
   * Creates an array of the own enumerable symbols of `object`.
   *
   * @private
   * @param {Object} object The object to query.
   * @returns {Array} Returns the array of symbols.
   */
  var getSymbols = !nativeGetSymbols ? stubArray : function (object) {
    if (object == null) {
      return [];
    }
    object = Object(object);
    return arrayFilter(nativeGetSymbols(object), function (symbol) {
      return propertyIsEnumerable$1.call(object, symbol);
    });
  };
  var getSymbols$1 = getSymbols;

  /**
   * The base implementation of `_.times` without support for iteratee shorthands
   * or max array length checks.
   *
   * @private
   * @param {number} n The number of times to invoke `iteratee`.
   * @param {Function} iteratee The function invoked per iteration.
   * @returns {Array} Returns the array of results.
   */
  function baseTimes(n, iteratee) {
    var index = -1,
      result = Array(n);
    while (++index < n) {
      result[index] = iteratee(index);
    }
    return result;
  }

  /**
   * Checks if `value` is object-like. A value is object-like if it's not `null`
   * and has a `typeof` result of "object".
   *
   * @static
   * @memberOf _
   * @since 4.0.0
   * @category Lang
   * @param {*} value The value to check.
   * @returns {boolean} Returns `true` if `value` is object-like, else `false`.
   * @example
   *
   * _.isObjectLike({});
   * // => true
   *
   * _.isObjectLike([1, 2, 3]);
   * // => true
   *
   * _.isObjectLike(_.noop);
   * // => false
   *
   * _.isObjectLike(null);
   * // => false
   */
  function isObjectLike(value) {
    return value != null && typeof value == 'object';
  }

  /** `Object#toString` result references. */
  var argsTag$2 = '[object Arguments]';

  /**
   * The base implementation of `_.isArguments`.
   *
   * @private
   * @param {*} value The value to check.
   * @returns {boolean} Returns `true` if `value` is an `arguments` object,
   */
  function baseIsArguments(value) {
    return isObjectLike(value) && baseGetTag(value) == argsTag$2;
  }

  /** Used for built-in method references. */
  var objectProto$6 = Object.prototype;

  /** Used to check objects for own properties. */
  var hasOwnProperty$5 = objectProto$6.hasOwnProperty;

  /** Built-in value references. */
  var propertyIsEnumerable = objectProto$6.propertyIsEnumerable;

  /**
   * Checks if `value` is likely an `arguments` object.
   *
   * @static
   * @memberOf _
   * @since 0.1.0
   * @category Lang
   * @param {*} value The value to check.
   * @returns {boolean} Returns `true` if `value` is an `arguments` object,
   *  else `false`.
   * @example
   *
   * _.isArguments(function() { return arguments; }());
   * // => true
   *
   * _.isArguments([1, 2, 3]);
   * // => false
   */
  var isArguments = baseIsArguments(function () {
    return arguments;
  }()) ? baseIsArguments : function (value) {
    return isObjectLike(value) && hasOwnProperty$5.call(value, 'callee') && !propertyIsEnumerable.call(value, 'callee');
  };
  var isArguments$1 = isArguments;

  /**
   * This method returns `false`.
   *
   * @static
   * @memberOf _
   * @since 4.13.0
   * @category Util
   * @returns {boolean} Returns `false`.
   * @example
   *
   * _.times(2, _.stubFalse);
   * // => [false, false]
   */
  function stubFalse() {
    return false;
  }

  /** Detect free variable `exports`. */
  var freeExports$1 = typeof exports == 'object' && exports && !exports.nodeType && exports;

  /** Detect free variable `module`. */
  var freeModule$1 = freeExports$1 && typeof module == 'object' && module && !module.nodeType && module;

  /** Detect the popular CommonJS extension `module.exports`. */
  var moduleExports$1 = freeModule$1 && freeModule$1.exports === freeExports$1;

  /** Built-in value references. */
  var Buffer = moduleExports$1 ? root$1.Buffer : undefined;

  /* Built-in method references for those with the same name as other `lodash` methods. */
  var nativeIsBuffer = Buffer ? Buffer.isBuffer : undefined;

  /**
   * Checks if `value` is a buffer.
   *
   * @static
   * @memberOf _
   * @since 4.3.0
   * @category Lang
   * @param {*} value The value to check.
   * @returns {boolean} Returns `true` if `value` is a buffer, else `false`.
   * @example
   *
   * _.isBuffer(new Buffer(2));
   * // => true
   *
   * _.isBuffer(new Uint8Array(2));
   * // => false
   */
  var isBuffer = nativeIsBuffer || stubFalse;
  var isBuffer$1 = isBuffer;

  /** Used as references for various `Number` constants. */
  var MAX_SAFE_INTEGER$1 = 9007199254740991;

  /** Used to detect unsigned integer values. */
  var reIsUint = /^(?:0|[1-9]\d*)$/;

  /**
   * Checks if `value` is a valid array-like index.
   *
   * @private
   * @param {*} value The value to check.
   * @param {number} [length=MAX_SAFE_INTEGER] The upper bounds of a valid index.
   * @returns {boolean} Returns `true` if `value` is a valid index, else `false`.
   */
  function isIndex(value, length) {
    var type = typeof value;
    length = length == null ? MAX_SAFE_INTEGER$1 : length;
    return !!length && (type == 'number' || type != 'symbol' && reIsUint.test(value)) && value > -1 && value % 1 == 0 && value < length;
  }

  /** Used as references for various `Number` constants. */
  var MAX_SAFE_INTEGER = 9007199254740991;

  /**
   * Checks if `value` is a valid array-like length.
   *
   * **Note:** This method is loosely based on
   * [`ToLength`](http://ecma-international.org/ecma-262/7.0/#sec-tolength).
   *
   * @static
   * @memberOf _
   * @since 4.0.0
   * @category Lang
   * @param {*} value The value to check.
   * @returns {boolean} Returns `true` if `value` is a valid length, else `false`.
   * @example
   *
   * _.isLength(3);
   * // => true
   *
   * _.isLength(Number.MIN_VALUE);
   * // => false
   *
   * _.isLength(Infinity);
   * // => false
   *
   * _.isLength('3');
   * // => false
   */
  function isLength(value) {
    return typeof value == 'number' && value > -1 && value % 1 == 0 && value <= MAX_SAFE_INTEGER;
  }

  /** `Object#toString` result references. */
  var argsTag$1 = '[object Arguments]',
    arrayTag$1 = '[object Array]',
    boolTag = '[object Boolean]',
    dateTag = '[object Date]',
    errorTag = '[object Error]',
    funcTag = '[object Function]',
    mapTag$1 = '[object Map]',
    numberTag = '[object Number]',
    objectTag$2 = '[object Object]',
    regexpTag = '[object RegExp]',
    setTag$1 = '[object Set]',
    stringTag = '[object String]',
    weakMapTag$1 = '[object WeakMap]';
  var arrayBufferTag = '[object ArrayBuffer]',
    dataViewTag$1 = '[object DataView]',
    float32Tag = '[object Float32Array]',
    float64Tag = '[object Float64Array]',
    int8Tag = '[object Int8Array]',
    int16Tag = '[object Int16Array]',
    int32Tag = '[object Int32Array]',
    uint8Tag = '[object Uint8Array]',
    uint8ClampedTag = '[object Uint8ClampedArray]',
    uint16Tag = '[object Uint16Array]',
    uint32Tag = '[object Uint32Array]';

  /** Used to identify `toStringTag` values of typed arrays. */
  var typedArrayTags = {};
  typedArrayTags[float32Tag] = typedArrayTags[float64Tag] = typedArrayTags[int8Tag] = typedArrayTags[int16Tag] = typedArrayTags[int32Tag] = typedArrayTags[uint8Tag] = typedArrayTags[uint8ClampedTag] = typedArrayTags[uint16Tag] = typedArrayTags[uint32Tag] = true;
  typedArrayTags[argsTag$1] = typedArrayTags[arrayTag$1] = typedArrayTags[arrayBufferTag] = typedArrayTags[boolTag] = typedArrayTags[dataViewTag$1] = typedArrayTags[dateTag] = typedArrayTags[errorTag] = typedArrayTags[funcTag] = typedArrayTags[mapTag$1] = typedArrayTags[numberTag] = typedArrayTags[objectTag$2] = typedArrayTags[regexpTag] = typedArrayTags[setTag$1] = typedArrayTags[stringTag] = typedArrayTags[weakMapTag$1] = false;

  /**
   * The base implementation of `_.isTypedArray` without Node.js optimizations.
   *
   * @private
   * @param {*} value The value to check.
   * @returns {boolean} Returns `true` if `value` is a typed array, else `false`.
   */
  function baseIsTypedArray(value) {
    return isObjectLike(value) && isLength(value.length) && !!typedArrayTags[baseGetTag(value)];
  }

  /**
   * The base implementation of `_.unary` without support for storing metadata.
   *
   * @private
   * @param {Function} func The function to cap arguments for.
   * @returns {Function} Returns the new capped function.
   */
  function baseUnary(func) {
    return function (value) {
      return func(value);
    };
  }

  /** Detect free variable `exports`. */
  var freeExports = typeof exports == 'object' && exports && !exports.nodeType && exports;

  /** Detect free variable `module`. */
  var freeModule = freeExports && typeof module == 'object' && module && !module.nodeType && module;

  /** Detect the popular CommonJS extension `module.exports`. */
  var moduleExports = freeModule && freeModule.exports === freeExports;

  /** Detect free variable `process` from Node.js. */
  var freeProcess = moduleExports && freeGlobal$1.process;

  /** Used to access faster Node.js helpers. */
  var nodeUtil = function () {
    try {
      // Use `util.types` for Node.js 10+.
      var types = freeModule && freeModule.require && freeModule.require('util').types;
      if (types) {
        return types;
      }

      // Legacy `process.binding('util')` for Node.js < 10.
      return freeProcess && freeProcess.binding && freeProcess.binding('util');
    } catch (e) {}
  }();
  var nodeUtil$1 = nodeUtil;

  /* Node.js helper references. */
  var nodeIsTypedArray = nodeUtil$1 && nodeUtil$1.isTypedArray;

  /**
   * Checks if `value` is classified as a typed array.
   *
   * @static
   * @memberOf _
   * @since 3.0.0
   * @category Lang
   * @param {*} value The value to check.
   * @returns {boolean} Returns `true` if `value` is a typed array, else `false`.
   * @example
   *
   * _.isTypedArray(new Uint8Array);
   * // => true
   *
   * _.isTypedArray([]);
   * // => false
   */
  var isTypedArray = nodeIsTypedArray ? baseUnary(nodeIsTypedArray) : baseIsTypedArray;
  var isTypedArray$1 = isTypedArray;

  /** Used for built-in method references. */
  var objectProto$5 = Object.prototype;

  /** Used to check objects for own properties. */
  var hasOwnProperty$4 = objectProto$5.hasOwnProperty;

  /**
   * Creates an array of the enumerable property names of the array-like `value`.
   *
   * @private
   * @param {*} value The value to query.
   * @param {boolean} inherited Specify returning inherited property names.
   * @returns {Array} Returns the array of property names.
   */
  function arrayLikeKeys(value, inherited) {
    var isArr = isArray$1(value),
      isArg = !isArr && isArguments$1(value),
      isBuff = !isArr && !isArg && isBuffer$1(value),
      isType = !isArr && !isArg && !isBuff && isTypedArray$1(value),
      skipIndexes = isArr || isArg || isBuff || isType,
      result = skipIndexes ? baseTimes(value.length, String) : [],
      length = result.length;
    for (var key in value) {
      if ((inherited || hasOwnProperty$4.call(value, key)) && !(skipIndexes && (
      // Safari 9 has enumerable `arguments.length` in strict mode.
      key == 'length' ||
      // Node.js 0.10 has enumerable non-index properties on buffers.
      isBuff && (key == 'offset' || key == 'parent') ||
      // PhantomJS 2 has enumerable non-index properties on typed arrays.
      isType && (key == 'buffer' || key == 'byteLength' || key == 'byteOffset') ||
      // Skip index properties.
      isIndex(key, length)))) {
        result.push(key);
      }
    }
    return result;
  }

  /** Used for built-in method references. */
  var objectProto$4 = Object.prototype;

  /**
   * Checks if `value` is likely a prototype object.
   *
   * @private
   * @param {*} value The value to check.
   * @returns {boolean} Returns `true` if `value` is a prototype, else `false`.
   */
  function isPrototype(value) {
    var Ctor = value && value.constructor,
      proto = typeof Ctor == 'function' && Ctor.prototype || objectProto$4;
    return value === proto;
  }

  /**
   * Creates a unary function that invokes `func` with its argument transformed.
   *
   * @private
   * @param {Function} func The function to wrap.
   * @param {Function} transform The argument transform.
   * @returns {Function} Returns the new function.
   */
  function overArg(func, transform) {
    return function (arg) {
      return func(transform(arg));
    };
  }

  /* Built-in method references for those with the same name as other `lodash` methods. */
  var nativeKeys = overArg(Object.keys, Object);
  var nativeKeys$1 = nativeKeys;

  /** Used for built-in method references. */
  var objectProto$3 = Object.prototype;

  /** Used to check objects for own properties. */
  var hasOwnProperty$3 = objectProto$3.hasOwnProperty;

  /**
   * The base implementation of `_.keys` which doesn't treat sparse arrays as dense.
   *
   * @private
   * @param {Object} object The object to query.
   * @returns {Array} Returns the array of property names.
   */
  function baseKeys(object) {
    if (!isPrototype(object)) {
      return nativeKeys$1(object);
    }
    var result = [];
    for (var key in Object(object)) {
      if (hasOwnProperty$3.call(object, key) && key != 'constructor') {
        result.push(key);
      }
    }
    return result;
  }

  /**
   * Checks if `value` is array-like. A value is considered array-like if it's
   * not a function and has a `value.length` that's an integer greater than or
   * equal to `0` and less than or equal to `Number.MAX_SAFE_INTEGER`.
   *
   * @static
   * @memberOf _
   * @since 4.0.0
   * @category Lang
   * @param {*} value The value to check.
   * @returns {boolean} Returns `true` if `value` is array-like, else `false`.
   * @example
   *
   * _.isArrayLike([1, 2, 3]);
   * // => true
   *
   * _.isArrayLike(document.body.children);
   * // => true
   *
   * _.isArrayLike('abc');
   * // => true
   *
   * _.isArrayLike(_.noop);
   * // => false
   */
  function isArrayLike(value) {
    return value != null && isLength(value.length) && !isFunction(value);
  }

  /**
   * Creates an array of the own enumerable property names of `object`.
   *
   * **Note:** Non-object values are coerced to objects. See the
   * [ES spec](http://ecma-international.org/ecma-262/7.0/#sec-object.keys)
   * for more details.
   *
   * @static
   * @since 0.1.0
   * @memberOf _
   * @category Object
   * @param {Object} object The object to query.
   * @returns {Array} Returns the array of property names.
   * @example
   *
   * function Foo() {
   *   this.a = 1;
   *   this.b = 2;
   * }
   *
   * Foo.prototype.c = 3;
   *
   * _.keys(new Foo);
   * // => ['a', 'b'] (iteration order is not guaranteed)
   *
   * _.keys('hi');
   * // => ['0', '1']
   */
  function keys(object) {
    return isArrayLike(object) ? arrayLikeKeys(object) : baseKeys(object);
  }

  /**
   * Creates an array of own enumerable property names and symbols of `object`.
   *
   * @private
   * @param {Object} object The object to query.
   * @returns {Array} Returns the array of property names and symbols.
   */
  function getAllKeys(object) {
    return baseGetAllKeys(object, keys, getSymbols$1);
  }

  /** Used to compose bitmasks for value comparisons. */
  var COMPARE_PARTIAL_FLAG$3 = 1;

  /** Used for built-in method references. */
  var objectProto$2 = Object.prototype;

  /** Used to check objects for own properties. */
  var hasOwnProperty$2 = objectProto$2.hasOwnProperty;

  /**
   * A specialized version of `baseIsEqualDeep` for objects with support for
   * partial deep comparisons.
   *
   * @private
   * @param {Object} object The object to compare.
   * @param {Object} other The other object to compare.
   * @param {number} bitmask The bitmask flags. See `baseIsEqual` for more details.
   * @param {Function} customizer The function to customize comparisons.
   * @param {Function} equalFunc The function to determine equivalents of values.
   * @param {Object} stack Tracks traversed `object` and `other` objects.
   * @returns {boolean} Returns `true` if the objects are equivalent, else `false`.
   */
  function equalObjects(object, other, bitmask, customizer, equalFunc, stack) {
    var isPartial = bitmask & COMPARE_PARTIAL_FLAG$3,
      objProps = getAllKeys(object),
      objLength = objProps.length,
      othProps = getAllKeys(other),
      othLength = othProps.length;
    if (objLength != othLength && !isPartial) {
      return false;
    }
    var index = objLength;
    while (index--) {
      var key = objProps[index];
      if (!(isPartial ? key in other : hasOwnProperty$2.call(other, key))) {
        return false;
      }
    }
    // Check that cyclic values are equal.
    var objStacked = stack.get(object);
    var othStacked = stack.get(other);
    if (objStacked && othStacked) {
      return objStacked == other && othStacked == object;
    }
    var result = true;
    stack.set(object, other);
    stack.set(other, object);
    var skipCtor = isPartial;
    while (++index < objLength) {
      key = objProps[index];
      var objValue = object[key],
        othValue = other[key];
      if (customizer) {
        var compared = isPartial ? customizer(othValue, objValue, key, other, object, stack) : customizer(objValue, othValue, key, object, other, stack);
      }
      // Recursively compare objects (susceptible to call stack limits).
      if (!(compared === undefined ? objValue === othValue || equalFunc(objValue, othValue, bitmask, customizer, stack) : compared)) {
        result = false;
        break;
      }
      skipCtor || (skipCtor = key == 'constructor');
    }
    if (result && !skipCtor) {
      var objCtor = object.constructor,
        othCtor = other.constructor;

      // Non `Object` object instances with different constructors are not equal.
      if (objCtor != othCtor && 'constructor' in object && 'constructor' in other && !(typeof objCtor == 'function' && objCtor instanceof objCtor && typeof othCtor == 'function' && othCtor instanceof othCtor)) {
        result = false;
      }
    }
    stack['delete'](object);
    stack['delete'](other);
    return result;
  }

  /* Built-in method references that are verified to be native. */
  var DataView = getNative(root$1, 'DataView');
  var DataView$1 = DataView;

  /* Built-in method references that are verified to be native. */
  var Promise$1 = getNative(root$1, 'Promise');
  var Promise$2 = Promise$1;

  /* Built-in method references that are verified to be native. */
  var Set = getNative(root$1, 'Set');
  var Set$1 = Set;

  /* Built-in method references that are verified to be native. */
  var WeakMap = getNative(root$1, 'WeakMap');
  var WeakMap$1 = WeakMap;

  /** `Object#toString` result references. */
  var mapTag = '[object Map]',
    objectTag$1 = '[object Object]',
    promiseTag = '[object Promise]',
    setTag = '[object Set]',
    weakMapTag = '[object WeakMap]';
  var dataViewTag = '[object DataView]';

  /** Used to detect maps, sets, and weakmaps. */
  var dataViewCtorString = toSource(DataView$1),
    mapCtorString = toSource(Map$1),
    promiseCtorString = toSource(Promise$2),
    setCtorString = toSource(Set$1),
    weakMapCtorString = toSource(WeakMap$1);

  /**
   * Gets the `toStringTag` of `value`.
   *
   * @private
   * @param {*} value The value to query.
   * @returns {string} Returns the `toStringTag`.
   */
  var getTag = baseGetTag;

  // Fallback for data views, maps, sets, and weak maps in IE 11 and promises in Node.js < 6.
  if (DataView$1 && getTag(new DataView$1(new ArrayBuffer(1))) != dataViewTag || Map$1 && getTag(new Map$1()) != mapTag || Promise$2 && getTag(Promise$2.resolve()) != promiseTag || Set$1 && getTag(new Set$1()) != setTag || WeakMap$1 && getTag(new WeakMap$1()) != weakMapTag) {
    getTag = function getTag(value) {
      var result = baseGetTag(value),
        Ctor = result == objectTag$1 ? value.constructor : undefined,
        ctorString = Ctor ? toSource(Ctor) : '';
      if (ctorString) {
        switch (ctorString) {
          case dataViewCtorString:
            return dataViewTag;
          case mapCtorString:
            return mapTag;
          case promiseCtorString:
            return promiseTag;
          case setCtorString:
            return setTag;
          case weakMapCtorString:
            return weakMapTag;
        }
      }
      return result;
    };
  }
  var getTag$1 = getTag;

  /** Used to compose bitmasks for value comparisons. */
  var COMPARE_PARTIAL_FLAG$2 = 1;

  /** `Object#toString` result references. */
  var argsTag = '[object Arguments]',
    arrayTag = '[object Array]',
    objectTag = '[object Object]';

  /** Used for built-in method references. */
  var objectProto$1 = Object.prototype;

  /** Used to check objects for own properties. */
  var hasOwnProperty$1 = objectProto$1.hasOwnProperty;

  /**
   * A specialized version of `baseIsEqual` for arrays and objects which performs
   * deep comparisons and tracks traversed objects enabling objects with circular
   * references to be compared.
   *
   * @private
   * @param {Object} object The object to compare.
   * @param {Object} other The other object to compare.
   * @param {number} bitmask The bitmask flags. See `baseIsEqual` for more details.
   * @param {Function} customizer The function to customize comparisons.
   * @param {Function} equalFunc The function to determine equivalents of values.
   * @param {Object} [stack] Tracks traversed `object` and `other` objects.
   * @returns {boolean} Returns `true` if the objects are equivalent, else `false`.
   */
  function baseIsEqualDeep(object, other, bitmask, customizer, equalFunc, stack) {
    var objIsArr = isArray$1(object),
      othIsArr = isArray$1(other),
      objTag = objIsArr ? arrayTag : getTag$1(object),
      othTag = othIsArr ? arrayTag : getTag$1(other);
    objTag = objTag == argsTag ? objectTag : objTag;
    othTag = othTag == argsTag ? objectTag : othTag;
    var objIsObj = objTag == objectTag,
      othIsObj = othTag == objectTag,
      isSameTag = objTag == othTag;
    if (isSameTag && isBuffer$1(object)) {
      if (!isBuffer$1(other)) {
        return false;
      }
      objIsArr = true;
      objIsObj = false;
    }
    if (isSameTag && !objIsObj) {
      stack || (stack = new Stack());
      return objIsArr || isTypedArray$1(object) ? equalArrays(object, other, bitmask, customizer, equalFunc, stack) : equalByTag(object, other, objTag, bitmask, customizer, equalFunc, stack);
    }
    if (!(bitmask & COMPARE_PARTIAL_FLAG$2)) {
      var objIsWrapped = objIsObj && hasOwnProperty$1.call(object, '__wrapped__'),
        othIsWrapped = othIsObj && hasOwnProperty$1.call(other, '__wrapped__');
      if (objIsWrapped || othIsWrapped) {
        var objUnwrapped = objIsWrapped ? object.value() : object,
          othUnwrapped = othIsWrapped ? other.value() : other;
        stack || (stack = new Stack());
        return equalFunc(objUnwrapped, othUnwrapped, bitmask, customizer, stack);
      }
    }
    if (!isSameTag) {
      return false;
    }
    stack || (stack = new Stack());
    return equalObjects(object, other, bitmask, customizer, equalFunc, stack);
  }

  /**
   * The base implementation of `_.isEqual` which supports partial comparisons
   * and tracks traversed objects.
   *
   * @private
   * @param {*} value The value to compare.
   * @param {*} other The other value to compare.
   * @param {boolean} bitmask The bitmask flags.
   *  1 - Unordered comparison
   *  2 - Partial comparison
   * @param {Function} [customizer] The function to customize comparisons.
   * @param {Object} [stack] Tracks traversed `value` and `other` objects.
   * @returns {boolean} Returns `true` if the values are equivalent, else `false`.
   */
  function baseIsEqual(value, other, bitmask, customizer, stack) {
    if (value === other) {
      return true;
    }
    if (value == null || other == null || !isObjectLike(value) && !isObjectLike(other)) {
      return value !== value && other !== other;
    }
    return baseIsEqualDeep(value, other, bitmask, customizer, baseIsEqual, stack);
  }

  /** Used to compose bitmasks for value comparisons. */
  var COMPARE_PARTIAL_FLAG$1 = 1,
    COMPARE_UNORDERED_FLAG$1 = 2;

  /**
   * The base implementation of `_.isMatch` without support for iteratee shorthands.
   *
   * @private
   * @param {Object} object The object to inspect.
   * @param {Object} source The object of property values to match.
   * @param {Array} matchData The property names, values, and compare flags to match.
   * @param {Function} [customizer] The function to customize comparisons.
   * @returns {boolean} Returns `true` if `object` is a match, else `false`.
   */
  function baseIsMatch(object, source, matchData, customizer) {
    var index = matchData.length,
      length = index,
      noCustomizer = !customizer;
    if (object == null) {
      return !length;
    }
    object = Object(object);
    while (index--) {
      var data = matchData[index];
      if (noCustomizer && data[2] ? data[1] !== object[data[0]] : !(data[0] in object)) {
        return false;
      }
    }
    while (++index < length) {
      data = matchData[index];
      var key = data[0],
        objValue = object[key],
        srcValue = data[1];
      if (noCustomizer && data[2]) {
        if (objValue === undefined && !(key in object)) {
          return false;
        }
      } else {
        var stack = new Stack();
        if (customizer) {
          var result = customizer(objValue, srcValue, key, object, source, stack);
        }
        if (!(result === undefined ? baseIsEqual(srcValue, objValue, COMPARE_PARTIAL_FLAG$1 | COMPARE_UNORDERED_FLAG$1, customizer, stack) : result)) {
          return false;
        }
      }
    }
    return true;
  }

  /**
   * Checks if `value` is suitable for strict equality comparisons, i.e. `===`.
   *
   * @private
   * @param {*} value The value to check.
   * @returns {boolean} Returns `true` if `value` if suitable for strict
   *  equality comparisons, else `false`.
   */
  function isStrictComparable(value) {
    return value === value && !isObject(value);
  }

  /**
   * Gets the property names, values, and compare flags of `object`.
   *
   * @private
   * @param {Object} object The object to query.
   * @returns {Array} Returns the match data of `object`.
   */
  function getMatchData(object) {
    var result = keys(object),
      length = result.length;
    while (length--) {
      var key = result[length],
        value = object[key];
      result[length] = [key, value, isStrictComparable(value)];
    }
    return result;
  }

  /**
   * A specialized version of `matchesProperty` for source values suitable
   * for strict equality comparisons, i.e. `===`.
   *
   * @private
   * @param {string} key The key of the property to get.
   * @param {*} srcValue The value to match.
   * @returns {Function} Returns the new spec function.
   */
  function matchesStrictComparable(key, srcValue) {
    return function (object) {
      if (object == null) {
        return false;
      }
      return object[key] === srcValue && (srcValue !== undefined || key in Object(object));
    };
  }

  /**
   * The base implementation of `_.matches` which doesn't clone `source`.
   *
   * @private
   * @param {Object} source The object of property values to match.
   * @returns {Function} Returns the new spec function.
   */
  function baseMatches(source) {
    var matchData = getMatchData(source);
    if (matchData.length == 1 && matchData[0][2]) {
      return matchesStrictComparable(matchData[0][0], matchData[0][1]);
    }
    return function (object) {
      return object === source || baseIsMatch(object, source, matchData);
    };
  }

  /** `Object#toString` result references. */
  var symbolTag = '[object Symbol]';

  /**
   * Checks if `value` is classified as a `Symbol` primitive or object.
   *
   * @static
   * @memberOf _
   * @since 4.0.0
   * @category Lang
   * @param {*} value The value to check.
   * @returns {boolean} Returns `true` if `value` is a symbol, else `false`.
   * @example
   *
   * _.isSymbol(Symbol.iterator);
   * // => true
   *
   * _.isSymbol('abc');
   * // => false
   */
  function isSymbol(value) {
    return typeof value == 'symbol' || isObjectLike(value) && baseGetTag(value) == symbolTag;
  }

  /** Used to match property names within property paths. */
  var reIsDeepProp = /\.|\[(?:[^[\]]*|(["'])(?:(?!\1)[^\\]|\\.)*?\1)\]/,
    reIsPlainProp = /^\w*$/;

  /**
   * Checks if `value` is a property name and not a property path.
   *
   * @private
   * @param {*} value The value to check.
   * @param {Object} [object] The object to query keys on.
   * @returns {boolean} Returns `true` if `value` is a property name, else `false`.
   */
  function isKey(value, object) {
    if (isArray$1(value)) {
      return false;
    }
    var type = typeof value;
    if (type == 'number' || type == 'symbol' || type == 'boolean' || value == null || isSymbol(value)) {
      return true;
    }
    return reIsPlainProp.test(value) || !reIsDeepProp.test(value) || object != null && value in Object(object);
  }

  /** Error message constants. */
  var FUNC_ERROR_TEXT = 'Expected a function';

  /**
   * Creates a function that memoizes the result of `func`. If `resolver` is
   * provided, it determines the cache key for storing the result based on the
   * arguments provided to the memoized function. By default, the first argument
   * provided to the memoized function is used as the map cache key. The `func`
   * is invoked with the `this` binding of the memoized function.
   *
   * **Note:** The cache is exposed as the `cache` property on the memoized
   * function. Its creation may be customized by replacing the `_.memoize.Cache`
   * constructor with one whose instances implement the
   * [`Map`](http://ecma-international.org/ecma-262/7.0/#sec-properties-of-the-map-prototype-object)
   * method interface of `clear`, `delete`, `get`, `has`, and `set`.
   *
   * @static
   * @memberOf _
   * @since 0.1.0
   * @category Function
   * @param {Function} func The function to have its output memoized.
   * @param {Function} [resolver] The function to resolve the cache key.
   * @returns {Function} Returns the new memoized function.
   * @example
   *
   * var object = { 'a': 1, 'b': 2 };
   * var other = { 'c': 3, 'd': 4 };
   *
   * var values = _.memoize(_.values);
   * values(object);
   * // => [1, 2]
   *
   * values(other);
   * // => [3, 4]
   *
   * object.a = 2;
   * values(object);
   * // => [1, 2]
   *
   * // Modify the result cache.
   * values.cache.set(object, ['a', 'b']);
   * values(object);
   * // => ['a', 'b']
   *
   * // Replace `_.memoize.Cache`.
   * _.memoize.Cache = WeakMap;
   */
  function memoize(func, resolver) {
    if (typeof func != 'function' || resolver != null && typeof resolver != 'function') {
      throw new TypeError(FUNC_ERROR_TEXT);
    }
    var memoized = function memoized() {
      var args = arguments,
        key = resolver ? resolver.apply(this, args) : args[0],
        cache = memoized.cache;
      if (cache.has(key)) {
        return cache.get(key);
      }
      var result = func.apply(this, args);
      memoized.cache = cache.set(key, result) || cache;
      return result;
    };
    memoized.cache = new (memoize.Cache || MapCache)();
    return memoized;
  }

  // Expose `MapCache`.
  memoize.Cache = MapCache;

  /** Used as the maximum memoize cache size. */
  var MAX_MEMOIZE_SIZE = 500;

  /**
   * A specialized version of `_.memoize` which clears the memoized function's
   * cache when it exceeds `MAX_MEMOIZE_SIZE`.
   *
   * @private
   * @param {Function} func The function to have its output memoized.
   * @returns {Function} Returns the new memoized function.
   */
  function memoizeCapped(func) {
    var result = memoize(func, function (key) {
      if (cache.size === MAX_MEMOIZE_SIZE) {
        cache.clear();
      }
      return key;
    });
    var cache = result.cache;
    return result;
  }

  /** Used to match property names within property paths. */
  var rePropName = /[^.[\]]+|\[(?:(-?\d+(?:\.\d+)?)|(["'])((?:(?!\2)[^\\]|\\.)*?)\2)\]|(?=(?:\.|\[\])(?:\.|\[\]|$))/g;

  /** Used to match backslashes in property paths. */
  var reEscapeChar = /\\(\\)?/g;

  /**
   * Converts `string` to a property path array.
   *
   * @private
   * @param {string} string The string to convert.
   * @returns {Array} Returns the property path array.
   */
  var stringToPath = memoizeCapped(function (string) {
    var result = [];
    if (string.charCodeAt(0) === 46 /* . */) {
      result.push('');
    }
    string.replace(rePropName, function (match, number, quote, subString) {
      result.push(quote ? subString.replace(reEscapeChar, '$1') : number || match);
    });
    return result;
  });
  var stringToPath$1 = stringToPath;

  /** Used as references for various `Number` constants. */
  var INFINITY$2 = 1 / 0;

  /** Used to convert symbols to primitives and strings. */
  var symbolProto = Symbol$2 ? Symbol$2.prototype : undefined,
    symbolToString = symbolProto ? symbolProto.toString : undefined;

  /**
   * The base implementation of `_.toString` which doesn't convert nullish
   * values to empty strings.
   *
   * @private
   * @param {*} value The value to process.
   * @returns {string} Returns the string.
   */
  function baseToString(value) {
    // Exit early for strings to avoid a performance hit in some environments.
    if (typeof value == 'string') {
      return value;
    }
    if (isArray$1(value)) {
      // Recursively convert values (susceptible to call stack limits).
      return arrayMap(value, baseToString) + '';
    }
    if (isSymbol(value)) {
      return symbolToString ? symbolToString.call(value) : '';
    }
    var result = value + '';
    return result == '0' && 1 / value == -INFINITY$2 ? '-0' : result;
  }

  /**
   * Converts `value` to a string. An empty string is returned for `null`
   * and `undefined` values. The sign of `-0` is preserved.
   *
   * @static
   * @memberOf _
   * @since 4.0.0
   * @category Lang
   * @param {*} value The value to convert.
   * @returns {string} Returns the converted string.
   * @example
   *
   * _.toString(null);
   * // => ''
   *
   * _.toString(-0);
   * // => '-0'
   *
   * _.toString([1, 2, 3]);
   * // => '1,2,3'
   */
  function toString(value) {
    return value == null ? '' : baseToString(value);
  }

  /**
   * Casts `value` to a path array if it's not one.
   *
   * @private
   * @param {*} value The value to inspect.
   * @param {Object} [object] The object to query keys on.
   * @returns {Array} Returns the cast property path array.
   */
  function castPath(value, object) {
    if (isArray$1(value)) {
      return value;
    }
    return isKey(value, object) ? [value] : stringToPath$1(toString(value));
  }

  /** Used as references for various `Number` constants. */
  var INFINITY$1 = 1 / 0;

  /**
   * Converts `value` to a string key if it's not a string or symbol.
   *
   * @private
   * @param {*} value The value to inspect.
   * @returns {string|symbol} Returns the key.
   */
  function toKey(value) {
    if (typeof value == 'string' || isSymbol(value)) {
      return value;
    }
    var result = value + '';
    return result == '0' && 1 / value == -INFINITY$1 ? '-0' : result;
  }

  /**
   * The base implementation of `_.get` without support for default values.
   *
   * @private
   * @param {Object} object The object to query.
   * @param {Array|string} path The path of the property to get.
   * @returns {*} Returns the resolved value.
   */
  function baseGet(object, path) {
    path = castPath(path, object);
    var index = 0,
      length = path.length;
    while (object != null && index < length) {
      object = object[toKey(path[index++])];
    }
    return index && index == length ? object : undefined;
  }

  /**
   * Gets the value at `path` of `object`. If the resolved value is
   * `undefined`, the `defaultValue` is returned in its place.
   *
   * @static
   * @memberOf _
   * @since 3.7.0
   * @category Object
   * @param {Object} object The object to query.
   * @param {Array|string} path The path of the property to get.
   * @param {*} [defaultValue] The value returned for `undefined` resolved values.
   * @returns {*} Returns the resolved value.
   * @example
   *
   * var object = { 'a': [{ 'b': { 'c': 3 } }] };
   *
   * _.get(object, 'a[0].b.c');
   * // => 3
   *
   * _.get(object, ['a', '0', 'b', 'c']);
   * // => 3
   *
   * _.get(object, 'a.b.c', 'default');
   * // => 'default'
   */
  function get(object, path, defaultValue) {
    var result = object == null ? undefined : baseGet(object, path);
    return result === undefined ? defaultValue : result;
  }

  /**
   * The base implementation of `_.hasIn` without support for deep paths.
   *
   * @private
   * @param {Object} [object] The object to query.
   * @param {Array|string} key The key to check.
   * @returns {boolean} Returns `true` if `key` exists, else `false`.
   */
  function baseHasIn(object, key) {
    return object != null && key in Object(object);
  }

  /**
   * Checks if `path` exists on `object`.
   *
   * @private
   * @param {Object} object The object to query.
   * @param {Array|string} path The path to check.
   * @param {Function} hasFunc The function to check properties.
   * @returns {boolean} Returns `true` if `path` exists, else `false`.
   */
  function hasPath(object, path, hasFunc) {
    path = castPath(path, object);
    var index = -1,
      length = path.length,
      result = false;
    while (++index < length) {
      var key = toKey(path[index]);
      if (!(result = object != null && hasFunc(object, key))) {
        break;
      }
      object = object[key];
    }
    if (result || ++index != length) {
      return result;
    }
    length = object == null ? 0 : object.length;
    return !!length && isLength(length) && isIndex(key, length) && (isArray$1(object) || isArguments$1(object));
  }

  /**
   * Checks if `path` is a direct or inherited property of `object`.
   *
   * @static
   * @memberOf _
   * @since 4.0.0
   * @category Object
   * @param {Object} object The object to query.
   * @param {Array|string} path The path to check.
   * @returns {boolean} Returns `true` if `path` exists, else `false`.
   * @example
   *
   * var object = _.create({ 'a': _.create({ 'b': 2 }) });
   *
   * _.hasIn(object, 'a');
   * // => true
   *
   * _.hasIn(object, 'a.b');
   * // => true
   *
   * _.hasIn(object, ['a', 'b']);
   * // => true
   *
   * _.hasIn(object, 'b');
   * // => false
   */
  function hasIn(object, path) {
    return object != null && hasPath(object, path, baseHasIn);
  }

  /** Used to compose bitmasks for value comparisons. */
  var COMPARE_PARTIAL_FLAG = 1,
    COMPARE_UNORDERED_FLAG = 2;

  /**
   * The base implementation of `_.matchesProperty` which doesn't clone `srcValue`.
   *
   * @private
   * @param {string} path The path of the property to get.
   * @param {*} srcValue The value to match.
   * @returns {Function} Returns the new spec function.
   */
  function baseMatchesProperty(path, srcValue) {
    if (isKey(path) && isStrictComparable(srcValue)) {
      return matchesStrictComparable(toKey(path), srcValue);
    }
    return function (object) {
      var objValue = get(object, path);
      return objValue === undefined && objValue === srcValue ? hasIn(object, path) : baseIsEqual(srcValue, objValue, COMPARE_PARTIAL_FLAG | COMPARE_UNORDERED_FLAG);
    };
  }

  /**
   * This method returns the first argument it receives.
   *
   * @static
   * @since 0.1.0
   * @memberOf _
   * @category Util
   * @param {*} value Any value.
   * @returns {*} Returns `value`.
   * @example
   *
   * var object = { 'a': 1 };
   *
   * console.log(_.identity(object) === object);
   * // => true
   */
  function identity(value) {
    return value;
  }

  /**
   * The base implementation of `_.property` without support for deep paths.
   *
   * @private
   * @param {string} key The key of the property to get.
   * @returns {Function} Returns the new accessor function.
   */
  function baseProperty(key) {
    return function (object) {
      return object == null ? undefined : object[key];
    };
  }

  /**
   * A specialized version of `baseProperty` which supports deep paths.
   *
   * @private
   * @param {Array|string} path The path of the property to get.
   * @returns {Function} Returns the new accessor function.
   */
  function basePropertyDeep(path) {
    return function (object) {
      return baseGet(object, path);
    };
  }

  /**
   * Creates a function that returns the value at `path` of a given object.
   *
   * @static
   * @memberOf _
   * @since 2.4.0
   * @category Util
   * @param {Array|string} path The path of the property to get.
   * @returns {Function} Returns the new accessor function.
   * @example
   *
   * var objects = [
   *   { 'a': { 'b': 2 } },
   *   { 'a': { 'b': 1 } }
   * ];
   *
   * _.map(objects, _.property('a.b'));
   * // => [2, 1]
   *
   * _.map(_.sortBy(objects, _.property(['a', 'b'])), 'a.b');
   * // => [1, 2]
   */
  function property(path) {
    return isKey(path) ? baseProperty(toKey(path)) : basePropertyDeep(path);
  }

  /**
   * The base implementation of `_.iteratee`.
   *
   * @private
   * @param {*} [value=_.identity] The value to convert to an iteratee.
   * @returns {Function} Returns the iteratee.
   */
  function baseIteratee(value) {
    // Don't store the `typeof` result in a variable to avoid a JIT bug in Safari 9.
    // See https://bugs.webkit.org/show_bug.cgi?id=156034 for more details.
    if (typeof value == 'function') {
      return value;
    }
    if (value == null) {
      return identity;
    }
    if (typeof value == 'object') {
      return isArray$1(value) ? baseMatchesProperty(value[0], value[1]) : baseMatches(value);
    }
    return property(value);
  }

  /**
   * Creates a base function for methods like `_.forIn` and `_.forOwn`.
   *
   * @private
   * @param {boolean} [fromRight] Specify iterating from right to left.
   * @returns {Function} Returns the new base function.
   */
  function createBaseFor(fromRight) {
    return function (object, iteratee, keysFunc) {
      var index = -1,
        iterable = Object(object),
        props = keysFunc(object),
        length = props.length;
      while (length--) {
        var key = props[fromRight ? length : ++index];
        if (iteratee(iterable[key], key, iterable) === false) {
          break;
        }
      }
      return object;
    };
  }

  /**
   * The base implementation of `baseForOwn` which iterates over `object`
   * properties returned by `keysFunc` and invokes `iteratee` for each property.
   * Iteratee functions may exit iteration early by explicitly returning `false`.
   *
   * @private
   * @param {Object} object The object to iterate over.
   * @param {Function} iteratee The function invoked per iteration.
   * @param {Function} keysFunc The function to get the keys of `object`.
   * @returns {Object} Returns `object`.
   */
  var baseFor = createBaseFor();
  var baseFor$1 = baseFor;

  /**
   * The base implementation of `_.forOwn` without support for iteratee shorthands.
   *
   * @private
   * @param {Object} object The object to iterate over.
   * @param {Function} iteratee The function invoked per iteration.
   * @returns {Object} Returns `object`.
   */
  function baseForOwn(object, iteratee) {
    return object && baseFor$1(object, iteratee, keys);
  }

  /**
   * Creates a `baseEach` or `baseEachRight` function.
   *
   * @private
   * @param {Function} eachFunc The function to iterate over a collection.
   * @param {boolean} [fromRight] Specify iterating from right to left.
   * @returns {Function} Returns the new base function.
   */
  function createBaseEach(eachFunc, fromRight) {
    return function (collection, iteratee) {
      if (collection == null) {
        return collection;
      }
      if (!isArrayLike(collection)) {
        return eachFunc(collection, iteratee);
      }
      var length = collection.length,
        index = fromRight ? length : -1,
        iterable = Object(collection);
      while (fromRight ? index-- : ++index < length) {
        if (iteratee(iterable[index], index, iterable) === false) {
          break;
        }
      }
      return collection;
    };
  }

  /**
   * The base implementation of `_.forEach` without support for iteratee shorthands.
   *
   * @private
   * @param {Array|Object} collection The collection to iterate over.
   * @param {Function} iteratee The function invoked per iteration.
   * @returns {Array|Object} Returns `collection`.
   */
  var baseEach = createBaseEach(baseForOwn);
  var baseEach$1 = baseEach;

  /**
   * The base implementation of `_.map` without support for iteratee shorthands.
   *
   * @private
   * @param {Array|Object} collection The collection to iterate over.
   * @param {Function} iteratee The function invoked per iteration.
   * @returns {Array} Returns the new mapped array.
   */
  function baseMap(collection, iteratee) {
    var index = -1,
      result = isArrayLike(collection) ? Array(collection.length) : [];
    baseEach$1(collection, function (value, key, collection) {
      result[++index] = iteratee(value, key, collection);
    });
    return result;
  }

  /**
   * Creates an array of values by running each element in `collection` thru
   * `iteratee`. The iteratee is invoked with three arguments:
   * (value, index|key, collection).
   *
   * Many lodash methods are guarded to work as iteratees for methods like
   * `_.every`, `_.filter`, `_.map`, `_.mapValues`, `_.reject`, and `_.some`.
   *
   * The guarded methods are:
   * `ary`, `chunk`, `curry`, `curryRight`, `drop`, `dropRight`, `every`,
   * `fill`, `invert`, `parseInt`, `random`, `range`, `rangeRight`, `repeat`,
   * `sampleSize`, `slice`, `some`, `sortBy`, `split`, `take`, `takeRight`,
   * `template`, `trim`, `trimEnd`, `trimStart`, and `words`
   *
   * @static
   * @memberOf _
   * @since 0.1.0
   * @category Collection
   * @param {Array|Object} collection The collection to iterate over.
   * @param {Function} [iteratee=_.identity] The function invoked per iteration.
   * @returns {Array} Returns the new mapped array.
   * @example
   *
   * function square(n) {
   *   return n * n;
   * }
   *
   * _.map([4, 8], square);
   * // => [16, 64]
   *
   * _.map({ 'a': 4, 'b': 8 }, square);
   * // => [16, 64] (iteration order is not guaranteed)
   *
   * var users = [
   *   { 'user': 'barney' },
   *   { 'user': 'fred' }
   * ];
   *
   * // The `_.property` iteratee shorthand.
   * _.map(users, 'user');
   * // => ['barney', 'fred']
   */
  function map(collection, iteratee) {
    var func = isArray$1(collection) ? arrayMap : baseMap;
    return func(collection, baseIteratee(iteratee));
  }

  var defineProperty = function () {
    try {
      var func = getNative(Object, 'defineProperty');
      func({}, '', {});
      return func;
    } catch (e) {}
  }();
  var defineProperty$1 = defineProperty;

  /**
   * The base implementation of `assignValue` and `assignMergeValue` without
   * value checks.
   *
   * @private
   * @param {Object} object The object to modify.
   * @param {string} key The key of the property to assign.
   * @param {*} value The value to assign.
   */
  function baseAssignValue(object, key, value) {
    if (key == '__proto__' && defineProperty$1) {
      defineProperty$1(object, key, {
        'configurable': true,
        'enumerable': true,
        'value': value,
        'writable': true
      });
    } else {
      object[key] = value;
    }
  }

  /**
   * A specialized version of `baseAggregator` for arrays.
   *
   * @private
   * @param {Array} [array] The array to iterate over.
   * @param {Function} setter The function to set `accumulator` values.
   * @param {Function} iteratee The iteratee to transform keys.
   * @param {Object} accumulator The initial aggregated object.
   * @returns {Function} Returns `accumulator`.
   */
  function arrayAggregator(array, setter, iteratee, accumulator) {
    var index = -1,
      length = array == null ? 0 : array.length;
    while (++index < length) {
      var value = array[index];
      setter(accumulator, value, iteratee(value), array);
    }
    return accumulator;
  }

  /**
   * Aggregates elements of `collection` on `accumulator` with keys transformed
   * by `iteratee` and values set by `setter`.
   *
   * @private
   * @param {Array|Object} collection The collection to iterate over.
   * @param {Function} setter The function to set `accumulator` values.
   * @param {Function} iteratee The iteratee to transform keys.
   * @param {Object} accumulator The initial aggregated object.
   * @returns {Function} Returns `accumulator`.
   */
  function baseAggregator(collection, setter, iteratee, accumulator) {
    baseEach$1(collection, function (value, key, collection) {
      setter(accumulator, value, iteratee(value), collection);
    });
    return accumulator;
  }

  /**
   * Creates a function like `_.groupBy`.
   *
   * @private
   * @param {Function} setter The function to set accumulator values.
   * @param {Function} [initializer] The accumulator object initializer.
   * @returns {Function} Returns the new aggregator function.
   */
  function createAggregator(setter, initializer) {
    return function (collection, iteratee) {
      var func = isArray$1(collection) ? arrayAggregator : baseAggregator,
        accumulator = initializer ? initializer() : {};
      return func(collection, setter, baseIteratee(iteratee), accumulator);
    };
  }

  /** Used for built-in method references. */
  var objectProto = Object.prototype;

  /** Used to check objects for own properties. */
  var hasOwnProperty = objectProto.hasOwnProperty;

  /**
   * Creates an object composed of keys generated from the results of running
   * each element of `collection` thru `iteratee`. The order of grouped values
   * is determined by the order they occur in `collection`. The corresponding
   * value of each key is an array of elements responsible for generating the
   * key. The iteratee is invoked with one argument: (value).
   *
   * @static
   * @memberOf _
   * @since 0.1.0
   * @category Collection
   * @param {Array|Object} collection The collection to iterate over.
   * @param {Function} [iteratee=_.identity] The iteratee to transform keys.
   * @returns {Object} Returns the composed aggregate object.
   * @example
   *
   * _.groupBy([6.1, 4.2, 6.3], Math.floor);
   * // => { '4': [4.2], '6': [6.1, 6.3] }
   *
   * // The `_.property` iteratee shorthand.
   * _.groupBy(['one', 'two', 'three'], 'length');
   * // => { '3': ['one', 'two'], '5': ['three'] }
   */
  var groupBy = createAggregator(function (result, value, key) {
    if (hasOwnProperty.call(result, key)) {
      result[key].push(value);
    } else {
      baseAssignValue(result, key, [value]);
    }
  });
  var _groupBy = groupBy;

  /**
   * Creates a `_.find` or `_.findLast` function.
   *
   * @private
   * @param {Function} findIndexFunc The function to find the collection index.
   * @returns {Function} Returns the new find function.
   */
  function createFind(findIndexFunc) {
    return function (collection, predicate, fromIndex) {
      var iterable = Object(collection);
      if (!isArrayLike(collection)) {
        var iteratee = baseIteratee(predicate);
        collection = keys(collection);
        predicate = function predicate(key) {
          return iteratee(iterable[key], key, iterable);
        };
      }
      var index = findIndexFunc(collection, predicate, fromIndex);
      return index > -1 ? iterable[iteratee ? collection[index] : index] : undefined;
    };
  }

  /**
   * The base implementation of `_.findIndex` and `_.findLastIndex` without
   * support for iteratee shorthands.
   *
   * @private
   * @param {Array} array The array to inspect.
   * @param {Function} predicate The function invoked per iteration.
   * @param {number} fromIndex The index to search from.
   * @param {boolean} [fromRight] Specify iterating from right to left.
   * @returns {number} Returns the index of the matched value, else `-1`.
   */
  function baseFindIndex(array, predicate, fromIndex, fromRight) {
    var length = array.length,
      index = fromIndex + (fromRight ? 1 : -1);
    while (fromRight ? index-- : ++index < length) {
      if (predicate(array[index], index, array)) {
        return index;
      }
    }
    return -1;
  }

  /** Used to match a single whitespace character. */
  var reWhitespace = /\s/;

  /**
   * Used by `_.trim` and `_.trimEnd` to get the index of the last non-whitespace
   * character of `string`.
   *
   * @private
   * @param {string} string The string to inspect.
   * @returns {number} Returns the index of the last non-whitespace character.
   */
  function trimmedEndIndex(string) {
    var index = string.length;
    while (index-- && reWhitespace.test(string.charAt(index))) {}
    return index;
  }

  /** Used to match leading whitespace. */
  var reTrimStart = /^\s+/;

  /**
   * The base implementation of `_.trim`.
   *
   * @private
   * @param {string} string The string to trim.
   * @returns {string} Returns the trimmed string.
   */
  function baseTrim(string) {
    return string ? string.slice(0, trimmedEndIndex(string) + 1).replace(reTrimStart, '') : string;
  }

  /** Used as references for various `Number` constants. */
  var NAN = 0 / 0;

  /** Used to detect bad signed hexadecimal string values. */
  var reIsBadHex = /^[-+]0x[0-9a-f]+$/i;

  /** Used to detect binary string values. */
  var reIsBinary = /^0b[01]+$/i;

  /** Used to detect octal string values. */
  var reIsOctal = /^0o[0-7]+$/i;

  /** Built-in method references without a dependency on `root`. */
  var freeParseInt = parseInt;

  /**
   * Converts `value` to a number.
   *
   * @static
   * @memberOf _
   * @since 4.0.0
   * @category Lang
   * @param {*} value The value to process.
   * @returns {number} Returns the number.
   * @example
   *
   * _.toNumber(3.2);
   * // => 3.2
   *
   * _.toNumber(Number.MIN_VALUE);
   * // => 5e-324
   *
   * _.toNumber(Infinity);
   * // => Infinity
   *
   * _.toNumber('3.2');
   * // => 3.2
   */
  function toNumber(value) {
    if (typeof value == 'number') {
      return value;
    }
    if (isSymbol(value)) {
      return NAN;
    }
    if (isObject(value)) {
      var other = typeof value.valueOf == 'function' ? value.valueOf() : value;
      value = isObject(other) ? other + '' : other;
    }
    if (typeof value != 'string') {
      return value === 0 ? value : +value;
    }
    value = baseTrim(value);
    var isBinary = reIsBinary.test(value);
    return isBinary || reIsOctal.test(value) ? freeParseInt(value.slice(2), isBinary ? 2 : 8) : reIsBadHex.test(value) ? NAN : +value;
  }

  /** Used as references for various `Number` constants. */
  var INFINITY = 1 / 0,
    MAX_INTEGER = 1.7976931348623157e+308;

  /**
   * Converts `value` to a finite number.
   *
   * @static
   * @memberOf _
   * @since 4.12.0
   * @category Lang
   * @param {*} value The value to convert.
   * @returns {number} Returns the converted number.
   * @example
   *
   * _.toFinite(3.2);
   * // => 3.2
   *
   * _.toFinite(Number.MIN_VALUE);
   * // => 5e-324
   *
   * _.toFinite(Infinity);
   * // => 1.7976931348623157e+308
   *
   * _.toFinite('3.2');
   * // => 3.2
   */
  function toFinite(value) {
    if (!value) {
      return value === 0 ? value : 0;
    }
    value = toNumber(value);
    if (value === INFINITY || value === -INFINITY) {
      var sign = value < 0 ? -1 : 1;
      return sign * MAX_INTEGER;
    }
    return value === value ? value : 0;
  }

  /**
   * Converts `value` to an integer.
   *
   * **Note:** This method is loosely based on
   * [`ToInteger`](http://www.ecma-international.org/ecma-262/7.0/#sec-tointeger).
   *
   * @static
   * @memberOf _
   * @since 4.0.0
   * @category Lang
   * @param {*} value The value to convert.
   * @returns {number} Returns the converted integer.
   * @example
   *
   * _.toInteger(3.2);
   * // => 3
   *
   * _.toInteger(Number.MIN_VALUE);
   * // => 0
   *
   * _.toInteger(Infinity);
   * // => 1.7976931348623157e+308
   *
   * _.toInteger('3.2');
   * // => 3
   */
  function toInteger(value) {
    var result = toFinite(value),
      remainder = result % 1;
    return result === result ? remainder ? result - remainder : result : 0;
  }

  /* Built-in method references for those with the same name as other `lodash` methods. */
  var nativeMax = Math.max;

  /**
   * This method is like `_.find` except that it returns the index of the first
   * element `predicate` returns truthy for instead of the element itself.
   *
   * @static
   * @memberOf _
   * @since 1.1.0
   * @category Array
   * @param {Array} array The array to inspect.
   * @param {Function} [predicate=_.identity] The function invoked per iteration.
   * @param {number} [fromIndex=0] The index to search from.
   * @returns {number} Returns the index of the found element, else `-1`.
   * @example
   *
   * var users = [
   *   { 'user': 'barney',  'active': false },
   *   { 'user': 'fred',    'active': false },
   *   { 'user': 'pebbles', 'active': true }
   * ];
   *
   * _.findIndex(users, function(o) { return o.user == 'barney'; });
   * // => 0
   *
   * // The `_.matches` iteratee shorthand.
   * _.findIndex(users, { 'user': 'fred', 'active': false });
   * // => 1
   *
   * // The `_.matchesProperty` iteratee shorthand.
   * _.findIndex(users, ['active', false]);
   * // => 0
   *
   * // The `_.property` iteratee shorthand.
   * _.findIndex(users, 'active');
   * // => 2
   */
  function findIndex(array, predicate, fromIndex) {
    var length = array == null ? 0 : array.length;
    if (!length) {
      return -1;
    }
    var index = fromIndex == null ? 0 : toInteger(fromIndex);
    if (index < 0) {
      index = nativeMax(length + index, 0);
    }
    return baseFindIndex(array, baseIteratee(predicate), index);
  }

  /**
   * Iterates over elements of `collection`, returning the first element
   * `predicate` returns truthy for. The predicate is invoked with three
   * arguments: (value, index|key, collection).
   *
   * @static
   * @memberOf _
   * @since 0.1.0
   * @category Collection
   * @param {Array|Object} collection The collection to inspect.
   * @param {Function} [predicate=_.identity] The function invoked per iteration.
   * @param {number} [fromIndex=0] The index to search from.
   * @returns {*} Returns the matched element, else `undefined`.
   * @example
   *
   * var users = [
   *   { 'user': 'barney',  'age': 36, 'active': true },
   *   { 'user': 'fred',    'age': 40, 'active': false },
   *   { 'user': 'pebbles', 'age': 1,  'active': true }
   * ];
   *
   * _.find(users, function(o) { return o.age < 40; });
   * // => object for 'barney'
   *
   * // The `_.matches` iteratee shorthand.
   * _.find(users, { 'age': 1, 'active': true });
   * // => object for 'pebbles'
   *
   * // The `_.matchesProperty` iteratee shorthand.
   * _.find(users, ['active', false]);
   * // => object for 'fred'
   *
   * // The `_.property` iteratee shorthand.
   * _.find(users, 'active');
   * // => object for 'barney'
   */
  var find = createFind(findIndex);
  var _find = find;

  var config$1 = new Proxy({}, {
    get: function get(target, prop) {
      var _window$$yooessential;
      return (_window$$yooessential = window.$yooessentials.yoocustomizer) == null ? void 0 : _window$$yooessential[prop];
    }
  });
  var Source = {
    getQuery: function getQuery(node) {
      var _node$source, _node$source$query;
      return ((_node$source = node.source) == null ? void 0 : (_node$source$query = _node$source.query) == null ? void 0 : _node$source$query.name) || '';
    },
    setQuery: function setQuery(node, name) {
      var _node$source2;
      if (name) {
        _yooessentials.set(node, 'source.query.name', name);
        Vue__default["default"]["delete"](node.source.query, 'arguments');
        this.setField(node, false);
        return;
      }
      if (node != null && (_node$source2 = node.source) != null && _node$source2.query) {
        Vue__default["default"]["delete"](node.source, 'query');
        if (uikitUtil.isEmpty(node.source)) {
          Vue__default["default"]["delete"](node, 'source');
        }
      }
    },
    getQueryArgs: function getQueryArgs(node) {
      var _node$source3, _node$source3$query;
      return (_node$source3 = node.source) == null ? void 0 : (_node$source3$query = _node$source3.query) == null ? void 0 : _node$source3$query.arguments;
    },
    getQueryArg: function getQueryArg(node, name) {
      var _this$getQueryArgs;
      return (_this$getQueryArgs = this.getQueryArgs(node)) == null ? void 0 : _this$getQueryArgs[name];
    },
    setQueryArg: function setQueryArg(node, name, value) {
      _yooessentials.set(node, "source.query.arguments." + name, value);
    },
    getField: function getField(node) {
      var _node$source4, _node$source4$query, _node$source4$query$f;
      return ((_node$source4 = node.source) == null ? void 0 : (_node$source4$query = _node$source4.query) == null ? void 0 : (_node$source4$query$f = _node$source4$query.field) == null ? void 0 : _node$source4$query$f.name) || '';
    },
    setField: function setField(node, name) {
      if (name) {
        _yooessentials.set(node, 'source.query.field', {
          name: name
        });
      } else {
        var _node$source5;
        Vue__default["default"]["delete"]((_node$source5 = node.source) == null ? void 0 : _node$source5.query, 'field');
      }
    },
    getFieldArgs: function getFieldArgs(node) {
      var _node$source6, _node$source6$query, _node$source6$query$f;
      return (_node$source6 = node.source) == null ? void 0 : (_node$source6$query = _node$source6.query) == null ? void 0 : (_node$source6$query$f = _node$source6$query.field) == null ? void 0 : _node$source6$query$f.arguments;
    },
    getFieldArg: function getFieldArg(node, name) {
      var _this$getFieldArgs;
      return (_this$getFieldArgs = this.getFieldArgs(node)) == null ? void 0 : _this$getFieldArgs[name];
    },
    setFieldArg: function setFieldArg(node, name, value) {
      _yooessentials.set(node, "source.query.field.arguments." + name, value);
    },
    getFieldDirective: function getFieldDirective(node, name) {
      var _node$source7, _node$source7$query, _node$source7$query$f;
      return _find((_node$source7 = node.source) == null ? void 0 : (_node$source7$query = _node$source7.query) == null ? void 0 : (_node$source7$query$f = _node$source7$query.field) == null ? void 0 : _node$source7$query$f.directives, {
        name: name
      });
    },
    getFieldDirectiveArgs: function getFieldDirectiveArgs(node, directive) {
      var _this$getFieldDirecti;
      return (_this$getFieldDirecti = this.getFieldDirective(node, directive)) == null ? void 0 : _this$getFieldDirecti.arguments;
    },
    getFieldDirectiveArg: function getFieldDirectiveArg(node, directive, name) {
      var _this$getFieldDirecti2;
      return (_this$getFieldDirecti2 = this.getFieldDirectiveArgs(node, directive)) == null ? void 0 : _this$getFieldDirecti2[name];
    },
    setFieldDirectiveArg: function setFieldDirectiveArg(node, directive, name, value) {
      var dir = this.getFieldDirective(node, directive);
      if (!dir) {
        var _node$source8, _node$source8$query, _node$source8$query$f;
        dir = {
          name: directive
        };
        if (!((_node$source8 = node.source) != null && (_node$source8$query = _node$source8.query) != null && (_node$source8$query$f = _node$source8$query.field) != null && _node$source8$query$f.directives)) {
          _yooessentials.set(node, 'source.query.field.directives', []);
        }
        node.source.query.field.directives.push(dir);
      }
      _yooessentials.set(dir, "arguments." + name, value);
    },
    getProp: function getProp(node, name) {
      var _node$source9, _node$source9$props;
      return (_node$source9 = node.source) == null ? void 0 : (_node$source9$props = _node$source9.props) == null ? void 0 : _node$source9$props[name];
    },
    setProp: function setProp(node, name, prop) {
      var defaults = getDefaults(this.getPropField(node, prop).fields);
      Vue__default["default"]["delete"](node.props, name);
      _yooessentials.set(node, "source.props." + name, _extends({}, defaults, prop));
    },
    removeProp: function removeProp(node, name) {
      var _node$source10, _node$source10$props;
      if (!(node != null && (_node$source10 = node.source) != null && (_node$source10$props = _node$source10.props) != null && _node$source10$props[name])) {
        return;
      }
      Vue__default["default"]["delete"](node.source.props, name);
      if (uikitUtil.isEmpty(node.source.props)) {
        Vue__default["default"]["delete"](node.source, 'props');
      }
      if (uikitUtil.isEmpty(node.source)) {
        Vue__default["default"]["delete"](node, 'source');
      }
    },
    /**
     * Returns the schema field for the queried source
     *
     * @param node object
     * @param prop object
     */
    getSourceField: function getSourceField(node) {
      var source = this.getQuery(node);
      var field = this.getField(node);
      var path = field ? source + "." + field : source;
      return _yooessentials.SourceExtended.schema.findField(path);
    },
    getPropField: function getPropField(node, prop) {
      var fields = this.getFieldsList(node);
      var propField = _find(fields, {
        value: prop == null ? void 0 : prop.name
      });
      return propField ? _extends({}, propField, {
        fields: [].concat(resolveFilters(propField), resolveArguments(propField))
      }) : null;
    },
    getFieldsList: function getFieldsList(node) {
      var field = this.getSourceField(node);
      return _yooessentials.SourceExtended.schema.getFields(field);
    },
    // Used in SourceFieldSelect field config
    getSourceFieldOptions: function getSourceFieldOptions(node) {
      var field = _yooessentials.SourceExtended.schema.findField(this.getQuery(node));
      if (field && field.type.kind !== 'LIST') {
        var fields = _yooessentials.SourceExtended.schema.getFields(field, 'LIST');
        return !uikitUtil.isEmpty(fields) && _groupBy(fields, 'group');
      }
      return false;
    }
  };
  function getDefaults(fields) {
    var defaults = {};
    for (var _iterator = _createForOfIteratorHelperLoose(fields), _step; !(_step = _iterator()).done;) {
      var field = _step.value;
      if ('default' in field) {
        _yooessentials.set(defaults, field.name, field["default"]);
      }
    }
    return defaults;
  }
  function resolveArguments(field) {
    if (field === void 0) {
      field = {};
    }
    return map(field.arguments, function (arg, name) {
      return _extends({
        name: "arguments." + name
      }, arg);
    });
  }
  function resolveFilters(field, defaultFilters) {
    if (field === void 0) {
      field = {};
    }
    if (defaultFilters === void 0) {
      defaultFilters = ['before', 'after', 'search', 'replace'];
    }
    var baseFilters = config$1.sources.filters;
    return [].concat(defaultFilters, prepareFields$1(field.filters)).map(function (name) {
      return uikitUtil.isString(name) ? _extends({
        name: "filters." + name
      }, baseFilters[name]) : name;
    }).filter(function (t) {
      return t;
    });
  }
  function prepareFields$1(fields) {
    return uikitUtil.isPlainObject(fields) ? map(fields, function (field, name) {
      return _extends({
        name: "filters." + name
      }, field);
    }) : fields || [];
  }

  var errors = {
    'empty-props': 'No field mapped',
    'empty-query': 'No query selected',
    'invalid-prop': 'Invalid field mapped',
    'invalid-query': 'Invalid query'
  };
  Vue__default["default"].events.on('contentItemTitle', function (e, node) {
    var invalid = hasInvalidSource(node, null, e.origin.Builder.view);
    if (invalid) {
      return Vue__default["default"].i18n.t(errors[invalid]);
    }
  }, -50);

  // remove core status
  Vue__default["default"].events.on('statusNode', function (_ref, node) {
    _ref.origin;
      var _ref$result = _ref.result,
      result = _ref$result === void 0 ? [] : _ref$result;
    return result.filter(function (status) {
      var _status$component;
      return ((_status$component = status.component) == null ? void 0 : _status$component.name) !== 'SourceIcon';
    });
  }, -50);

  // extended status
  Vue__default["default"].events.on('statusNode', function (_ref2, node) {
    var _Node$type;
    var Node = _ref2.origin,
      _ref2$result = _ref2.result,
      result = _ref2$result === void 0 ? [] : _ref2$result;
    var query = _yooessentials.SourceExtended.hasQuery(node);
    var error = hasInvalidSource(node, null, Node.Builder.view);
    if (((_Node$type = Node.type) != null && _Node$type.element || Node.type.name === 'row') && Node.children.length) {
      query = Node.children.every(function (child) {
        return _yooessentials.SourceExtended.hasQuery(child);
      });
      error = error || Node.children.map(function (child) {
        return hasInvalidSource(child, null, Node.Builder.view);
      })[0];
    }
    query = query || _yooessentials.SourceExtended.hasProps(node);
    if (query || error) {
      result.push({
        component: __vue_component__$7,
        error: errors[error]
      });
    }
    return result;
  });
  function hasInvalidSource(node, prop, view) {
    var _window$$yooess, _props$prop;
    var isNode = !prop;
    var props = _yooessentials.SourceExtended.getProps(node);
    var hasProps = prop ? false : !uikitUtil.isEmpty(props);
    var origin = _yooessentials.SourceExtended.getQueryOrigin(node, prop);
    var builder = (_window$$yooess = window.$yooess) == null ? void 0 : _window$$yooess.yoobuilder;
    var isAdjacent = !builder.exists(node);
    if (hasProps) {
      return Object.keys(props).filter(function (prop) {
        return !prop.startsWith('_condition');
      }) // skip core condition props
      .map(function (prop) {
        return hasInvalidSource(node, prop, view);
      }).shift() || false;
    }
    if (prop && !origin && builder.exists(node)) {
      return 'empty-query';
    }
    if (origin.match(/inherit\.node\.query/) && !origin.match(/query\.(.){2,}$/) && builder.exists(node)) {
      return 'empty-query';
    }
    if (origin != null && origin.match(/global-query/)) {
      var _origin$match = origin.match(/query\.(.*)$/),
        id = _origin$match[1];
      if (!_yooessentials.Dynamic.getGlobalQuery(id)) {
        return 'invalid-query';
      }
    }
    if (origin != null && origin.match(/inherit\.closest/)) {
      if (!_yooessentials.SourceExtended.getClosestInheritableNode(node)) {
        return 'invalid-query';
      }
    }
    var query = _yooessentials.SourceExtended.getQuery(node, prop);
    if (!query) {
      return false;
    }
    if (!_yooessentials.SourceExtended.getSourceField(query)) {
      return 'invalid-query';
    }
    if (view) {
      var _field$metadata;
      var field = _yooessentials.SourceExtended.schema.findField(query.name);
      if (field != null && (_field$metadata = field.metadata) != null && _field$metadata.view && !uikitUtil.includes(field.metadata.view, view)) {
        return 'invalid-query';
      }
    }
    if (prop && !_yooessentials.SourceExtended.getPropField(query, (_props$prop = props[prop]) == null ? void 0 : _props$prop.name)) {
      return 'invalid-prop';
    }
    if (isNode && !isAdjacent && !hasProps) {
      var itHasInheritingProps = _yooessentials.flattenChildren(node).some(function (child) {
        return hasInheritingProps(child);
      });
      if (!itHasInheritingProps) {
        return 'empty-props';
      }
    }
    return false;
  }
  function hasInheritingProps(node) {
    var props = _yooessentials.SourceExtended.getProps(node);
    return Object.keys(props).some(function (key) {
      var _props$key, _props$key$query;
      return (_props$key = props[key]) == null ? void 0 : (_props$key$query = _props$key.query) == null ? void 0 : _props$key$query.inherit;
    });
  }

  Vue__default["default"].events.on('openPanel', function (e, panel) {
    var _panel$component, _panel$component2, _panel$component2$mix;
    if ((panel == null ? void 0 : (_panel$component = panel.component) == null ? void 0 : _panel$component.name) === 'BuilderPanel' || panel != null && (_panel$component2 = panel.component) != null && (_panel$component2$mix = _panel$component2.mixins) != null && _panel$component2$mix.find(function (c) {
      return c.name === 'BuilderPanel';
    })) {
      panel.component = {
        mixins: [panel.component, DynamicBuilderPanel]
      };
    }
  }, -100);
  Vue__default["default"].events.on('yooessentialsBuilderPrepareField', function (e, _ref) {
    var node = _ref.node,
      field = _ref.field;
    if (!field.source) {
      return;
    }
    field.buttons = [].concat((field.buttons || []).filter(function (btn) {
      return btn.action !== 'pickSource';
    }), [{
      label: Vue__default["default"].i18n.t('Dynamic'),
      action: 'yooessentialsPickDynamic'
    }]);

    // support for default source attr
    // if (field.source_default?.query && !SourceExtended.getProp(this.node, field.name)) {
    //     const source = field.source_default;
    //     const defaults = SourceExtended.getPropDefaults(this.node, source.query, source.name);
    //     set(this.node, `source_extended.props.${field.name}`, {...defaults, ...source});
    // }

    if (_yooessentials.SourceExtended.getProp(node, field.name)) {
      field.component = __vue_component__$4;
    }
  });

  Vue__default["default"].events.on('yooessentialsPickDynamic', function (_ref, field, _ref2) {
    var origin = _ref.origin;
    var target = _ref2.target;
    var node = origin.node;
    origin.$dropdown(__vue_component__$2, {
      field: field,
      node: node
    }).show(target, {
      classes: 'yo-dropdown',
      boundaryX: uikitUtil.closest(target, '.yo-sidebar-fields > *') || target
    });
  });
  Vue__default["default"].events.on('yooessentialsSourceQueryOrigin', function (_ref3, _ref4) {
    var _prop, _prop$query, _prop2, _prop2$query, _prop3, _prop3$query, _prop4, _prop4$query, _SourceExtended$getQu;
    var result = _ref3.result;
    var node = _ref4.node,
      prop = _ref4.prop;
    if (result) {
      return result;
    }
    prop = _yooessentials.SourceExtended.getProp(node, prop);
    if ((_prop = prop) != null && (_prop$query = _prop.query) != null && _prop$query.inherit) {
      return "inherit." + prop.query.inherit;
    }
    if ((_prop2 = prop) != null && (_prop2$query = _prop2.query) != null && _prop2$query.global) {
      return "global-query." + prop.query.global;
    }
    if (_yooessentials.Dynamic.isGlobalSource((_prop3 = prop) == null ? void 0 : (_prop3$query = _prop3.query) == null ? void 0 : _prop3$query.name)) {
      return "global." + prop.query.name;
    }
    if ((_prop4 = prop) != null && (_prop4$query = _prop4.query) != null && _prop4$query.name) {
      var _SourceExtended$getSo, _SourceExtended$getSo2;
      if (((_SourceExtended$getSo = _yooessentials.SourceExtended.getSourceField(prop.query)) == null ? void 0 : (_SourceExtended$getSo2 = _SourceExtended$getSo.metadata) == null ? void 0 : _SourceExtended$getSo2.group) === 'Page') {
        return 'query.page';
      }
      return 'query';
    }
    if ((_SourceExtended$getQu = _yooessentials.SourceExtended.getQueryObject(node)) != null && _SourceExtended$getQu.name) {
      return 'inherit.node';
    }
  }, {
    priority: 10
  });

  /**
   * inherit node
   */
  Vue__default["default"].events.on('yooessentialsDynamicOptions', function (_ref, _ref2) {
    var _query, _SourceExtended$getSo;
    var _ref$result = _ref.result,
      result = _ref$result === void 0 ? [] : _ref$result;
    var node = _ref2.node,
      field = _ref2.field;
    var query = _yooessentials.SourceExtended.getQueryObject(node);
    if (!query) {
      return result;
    }
    if ((_query = query) != null && _query.global) {
      var _Dynamic$getGlobalQue, _Dynamic$getGlobalQue2;
      query = (_Dynamic$getGlobalQue = _yooessentials.Dynamic.getGlobalQuery(query.global)) == null ? void 0 : (_Dynamic$getGlobalQue2 = _Dynamic$getGlobalQue.source) == null ? void 0 : _Dynamic$getGlobalQue2.query;
    }
    result.push({
      query: query,
      group: 'Inherit',
      name: 'inherit.node',
      title: 'Node',
      meta: (_SourceExtended$getSo = _yooessentials.SourceExtended.getSourceField(query)) == null ? void 0 : _SourceExtended$getSo.metadata.label,
      fields: function fields() {
        return _yooessentials.SourceExtended.getFieldsList(query);
      },
      match: function match(_ref3) {
        var prop = _ref3.prop;
        var query = _yooessentials.SourceExtended.getQueryObject(node);
        if (query && !prop) {
          return true;
        }
        return query && prop && !(prop != null && prop.query);
      },
      resolve: function resolve(_ref4) {
        var mappedTo = _ref4.mappedTo;
        _yooessentials.SourceExtended.setProp(node, field.name, {
          name: mappedTo
        });
      }
    });
    return result;
  });

  /**
   * inherit closest
   */
  Vue__default["default"].events.on('yooessentialsDynamicOptions', function (_ref5, _ref6) {
    var _query2, _SourceExtended$getSo2;
    var _ref5$result = _ref5.result,
      result = _ref5$result === void 0 ? [] : _ref5$result;
    var node = _ref6.node,
      field = _ref6.field;
    var closest = _yooessentials.SourceExtended.getClosestInheritableNode(node);
    if (!closest) {
      return result;
    }
    var query = _yooessentials.SourceExtended.getQueryObject(closest);
    if ((_query2 = query) != null && _query2.global) {
      var _Dynamic$getGlobalQue3, _Dynamic$getGlobalQue4;
      query = (_Dynamic$getGlobalQue3 = _yooessentials.Dynamic.getGlobalQuery(query.global)) == null ? void 0 : (_Dynamic$getGlobalQue4 = _Dynamic$getGlobalQue3.source) == null ? void 0 : _Dynamic$getGlobalQue4.query;
    }
    result.push({
      query: query,
      group: 'Inherit',
      name: 'inherit.closest',
      title: 'Closest Node',
      meta: (_SourceExtended$getSo2 = _yooessentials.SourceExtended.getSourceField(query)) == null ? void 0 : _SourceExtended$getSo2.metadata.label,
      fields: function fields() {
        return _yooessentials.SourceExtended.getFieldsList(query);
      },
      match: function match() {
        var _prop$query;
        var prop = _yooessentials.SourceExtended.getProp(node, field.name);
        return (prop == null ? void 0 : (_prop$query = prop.query) == null ? void 0 : _prop$query.inherit) === 'closest';
      },
      resolve: function resolve(_ref7) {
        var mappedTo = _ref7.mappedTo;
        _yooessentials.SourceExtended.setExtendedProp(node, field.name, {
          name: mappedTo,
          query: {
            inherit: 'closest'
          }
        }, query);
      }
    });
    return result;
  });

  /**
   * page queries
   */
  Vue__default["default"].events.on('yooessentialsDynamicOptions', function (_ref8, _ref9) {
    var _field$sourceOmitOrig;
    var _ref8$result = _ref8.result,
      result = _ref8$result === void 0 ? [] : _ref8$result;
    var node = _ref9.node,
      field = _ref9.field;
    if (uikitUtil.includes((_field$sourceOmitOrig = field.sourceOmitOrigin) != null ? _field$sourceOmitOrig : [], 'query.page')) {
      return result;
    }
    var _loop = function _loop(source) {
      var _SourceExtended$schem;
      var query = {
        name: source
      };
      result.push({
        query: query,
        group: 'Page',
        name: "page." + source,
        title: (_SourceExtended$schem = _yooessentials.SourceExtended.schema.findField(source)) == null ? void 0 : _SourceExtended$schem.metadata.label,
        fields: function fields() {
          return _yooessentials.SourceExtended.getFieldsList(query);
        },
        match: function match() {
          var _prop$query2;
          var prop = _yooessentials.SourceExtended.getProp(node, field.name);
          return (prop == null ? void 0 : (_prop$query2 = prop.query) == null ? void 0 : _prop$query2.name) === source;
        },
        resolve: function resolve(_ref10) {
          var mappedTo = _ref10.mappedTo;
          _yooessentials.SourceExtended.setExtendedProp(node, field.name, {
            name: mappedTo,
            query: query
          }, query);
        }
      });
    };
    for (var source in _yooessentials.Dynamic.pageQueries) {
      _loop(source);
    }
    return result;
  });

  /**
   * global sources
   */
  Vue__default["default"].events.on('yooessentialsDynamicOptions', function (_ref11, _ref12) {
    var _ref11$result = _ref11.result,
      result = _ref11$result === void 0 ? [] : _ref11$result;
    var node = _ref12.node,
      field = _ref12.field;
    var _loop2 = function _loop2(source) {
      var _SourceExtended$schem2;
      var query = {
        name: source
      };
      result.push({
        query: query,
        group: 'Global',
        name: "global." + source,
        title: (_SourceExtended$schem2 = _yooessentials.SourceExtended.schema.findField(source)) == null ? void 0 : _SourceExtended$schem2.metadata.label,
        fields: function fields() {
          return _yooessentials.SourceExtended.getFieldsList(query);
        },
        match: function match() {
          var _prop$query3;
          var prop = _yooessentials.SourceExtended.getProp(node, field.name);
          return (prop == null ? void 0 : (_prop$query3 = prop.query) == null ? void 0 : _prop$query3.name) === source;
        },
        resolve: function resolve(_ref13) {
          var mappedTo = _ref13.mappedTo;
          _yooessentials.SourceExtended.setExtendedProp(node, field.name, {
            name: mappedTo,
            query: query
          }, query);
        }
      });
    };
    for (var source in _yooessentials.Dynamic.globalSources) {
      _loop2(source);
    }
    return result;
  });

  /**
   * global queries
   */
  Vue__default["default"].events.on('yooessentialsDynamicOptions', function (_ref14, _ref15) {
    var _ref14$result = _ref14.result,
      result = _ref14$result === void 0 ? [] : _ref14$result;
    var node = _ref15.node,
      field = _ref15.field;
    var _loop3 = function _loop3() {
      var _globalQuery$source, _SourceExtended$getSo3;
      var globalQuery = _step.value;
      var query = globalQuery == null ? void 0 : (_globalQuery$source = globalQuery.source) == null ? void 0 : _globalQuery$source.query;
      result.push({
        query: query,
        group: 'Global',
        name: "global-query." + globalQuery.id,
        title: globalQuery.name,
        meta: (_SourceExtended$getSo3 = _yooessentials.SourceExtended.getSourceField(query)) == null ? void 0 : _SourceExtended$getSo3.metadata.label,
        fields: function fields() {
          return _yooessentials.SourceExtended.getFieldsList(query);
        },
        match: function match() {
          var _prop$query4;
          var prop = _yooessentials.SourceExtended.getProp(node, field.name);
          return (prop == null ? void 0 : (_prop$query4 = prop.query) == null ? void 0 : _prop$query4.global) === globalQuery.id;
        },
        resolve: function resolve(_ref16) {
          var mappedTo = _ref16.mappedTo;
          _yooessentials.SourceExtended.setExtendedProp(node, field.name, {
            name: mappedTo,
            query: {
              global: globalQuery.id
            }
          }, query);
        }
      });
    };
    for (var _iterator = _createForOfIteratorHelperLoose(_yooessentials.Dynamic.queries), _step; !(_step = _iterator()).done;) {
      _loop3();
    }
    return result;
  });

  /**
   * custom query
   */
  Vue__default["default"].events.on('yooessentialsDynamicOptions', function (_ref17, _ref18) {
    var _SourceExtended$getPr;
    var _ref17$result = _ref17.result,
      result = _ref17$result === void 0 ? [] : _ref17$result;
    var node = _ref18.node,
      field = _ref18.field;
    var origin = 'query';
    var currentOrigin = _yooessentials.SourceExtended.getQueryOrigin(node, field.name);
    var query = (_SourceExtended$getPr = _yooessentials.SourceExtended.getProp(node, field.name)) == null ? void 0 : _SourceExtended$getPr.query;
    var queryField = _yooessentials.SourceExtended.getSourceField(query);
    var matched = origin === currentOrigin;
    result.push({
      query: query,
      group: '',
      name: 'query',
      title: 'Custom Query',
      meta: matched && (queryField == null ? void 0 : queryField.metadata.label),
      component: __vue_component__,
      match: function match() {
        return matched;
      },
      resolve: function resolve(_ref19) {
        var mappedTo = _ref19.mappedTo,
          query = _ref19.query;
        _yooessentials.SourceExtended.setExtendedProp(node, field.name, {
          name: mappedTo,
          query: query
        }, query);
      }
    });
    return result;
  });

  var SourceSelect = {
    name: 'SourceSelect',
    "extends": $fields__default["default"].FieldSelect,
    inject: ['Builder', '$node'],
    computed: {
      value: {
        get: function get() {
          var query = _yooessentials.SourceExtended.getQueryObject(this.$node);
          return (query == null ? void 0 : query.name) || (query == null ? void 0 : query.global) || '';
        },
        set: function set(name) {
          _yooessentials.SourceExtended.setQuery(this.$node, name);
        }
      },
      coreFields: function coreFields() {
        var _SourceExtended$schem,
          _this = this;
        return (_SourceExtended$schem = _yooessentials.SourceExtended.schema.rootQueryFields) == null ? void 0 : _SourceExtended$schem.filter(function (field) {
          var _field$metadata, _this$Builder;
          return filterByView((_field$metadata = field.metadata) == null ? void 0 : _field$metadata.view, (_this$Builder = _this.Builder) == null ? void 0 : _this$Builder.view);
        }).map(function (_ref) {
          var name = _ref.name,
            _ref$metadata = _ref.metadata;
          _ref$metadata = _ref$metadata === void 0 ? {} : _ref$metadata;
          var label = _ref$metadata.label,
            _ref$metadata$group = _ref$metadata.group,
            group = _ref$metadata$group === void 0 ? 'Global' : _ref$metadata$group;
          return {
            value: name,
            text: label || name,
            group: group
          };
        });
      },
      globalFields: function globalFields() {
        return _yooessentials.Dynamic.queries.map(function (_ref2) {
          var id = _ref2.id,
            name = _ref2.name;
          return {
            value: id,
            text: name,
            group: 'Global'
          };
        });
      },
      fields: function fields() {
        var omittedGroups = this.field.omitgroups;
        return [].concat(this.coreFields.filter(function (_ref3) {
          var group = _ref3.group;
          return group === 'Page';
        }), this.coreFields.filter(function (_ref4) {
          var group = _ref4.group;
          return group === 'Global';
        }), this.globalFields, this.coreFields.filter(function (_ref5) {
          var group = _ref5.group;
          return !group.match(/Global|Page/);
        })).filter(function (field) {
          return !omittedGroups || !omittedGroups.includes(field.group);
        });
      }
    },
    methods: {
      filterOptions: function filterOptions() {
        var fields = _yooessentials.groupBy(this.fields, 'group');
        return [{
          text: this.$t('None'),
          value: ''
        }].concat(Object.keys(fields).map(function (key) {
          return {
            label: key,
            options: fields[key]
          };
        }));
      }
    }
  };
  function filterByView(views, view) {
    return !views || uikitUtil.includes(views, view);
  }

  var fields = /*#__PURE__*/Object.freeze({
    __proto__: null,
    FieldYooessentialsSourceSelect: SourceSelect
  });

  var DynamicModel = {
    name: 'DynamicModel',
    computed: {
      queries: {
        get: function get() {
          return _yooessentials.Config.get('dynamic.queries', []);
        },
        set: function set(values) {
          _yooessentials.Config.set('dynamic.queries', values);
        },
        cache: false
      },
      globalSources: function globalSources() {
        var _SourceExtended$schem;
        return _yooessentials.keyBy((_SourceExtended$schem = _yooessentials.SourceExtended.schema.rootQueryFields) == null ? void 0 : _SourceExtended$schem.filter(function (field) {
          var _field$metadata;
          return !(field != null && (_field$metadata = field.metadata) != null && _field$metadata.group);
        }), 'name');
      },
      pageQueries: function pageQueries() {
        var _SourceExtended$schem2;
        var queries = (_SourceExtended$schem2 = _yooessentials.SourceExtended.schema.rootQueryFields) == null ? void 0 : _SourceExtended$schem2.filter(function (field) {
          var _field$metadata2, _field$metadata$view, _field$metadata3;
          return (field == null ? void 0 : (_field$metadata2 = field.metadata) == null ? void 0 : _field$metadata2.group) === 'Page' && uikitUtil.includes((_field$metadata$view = field == null ? void 0 : (_field$metadata3 = field.metadata) == null ? void 0 : _field$metadata3.view) != null ? _field$metadata$view : [], _yooessentials.yoobuilder == null ? void 0 : _yooessentials.yoobuilder.view);
        });
        return _yooessentials.keyBy(queries, 'name');
      }
    },
    methods: {
      isGlobalSource: function isGlobalSource(name) {
        return Boolean(this.globalSources[name]);
      },
      resolveOriginQueryName: function resolveOriginQueryName(node, origin) {
        var _SourceExtended$getSo;
        var query = this.resolveOriginQuery(node, origin);
        return (_SourceExtended$getSo = _yooessentials.SourceExtended.getSourceField(query)) == null ? void 0 : _SourceExtended$getSo.metadata.label;
      },
      resolveOriginQuery: function resolveOriginQuery(node, origin) {
        return _yooessentials.SourceExtended.resolveOriginQuery(node, origin);
      },
      getGlobalQuery: function getGlobalQuery(id) {
        return this.queries.find(function (q) {
          return q.id === id;
        });
      },
      saveGlobalQuery: function saveGlobalQuery(values) {
        var queries = [].concat(this.queries);
        if (values.id) {
          queries[queries.findIndex(function (_ref) {
            var id = _ref.id;
            return id === values.id;
          })] = values;
        } else {
          values = _extends({}, values, {
            id: _yooessentials.uuid(),
            created_at: Date.now(),
            created_on: _yooessentials.get(window, '$customizer.yooessentials.version')
          });
          queries.push(values);
        }
        this.queries = queries;
        return values;
      },
      removeGlobalQuery: function removeGlobalQuery(query) {
        var queries = [].concat(this.queries);
        queries.splice(queries.indexOf(query), 1);
        this.queries = queries;
      }
    }
  };

  var Schema = /*#__PURE__*/function () {
    function Schema(schema) {
      this.schema = schema;
    }
    var _proto = Schema.prototype;
    _proto.setSchema = function setSchema(schema) {
      this.schema = schema;
    };
    _proto.findField = function findField(path) {
      return _findField(this.types, this.rootQueryType, path);
    };
    _proto.getFields = function getFields(field, fieldType) {
      if (fieldType === void 0) {
        fieldType = 'SCALAR';
      }
      return field ? _getFields(this.types, field, fieldType) : [];
    };
    _createClass(Schema, [{
      key: "types",
      get: function get() {
        var _this$schema;
        return (_this$schema = this.schema) == null ? void 0 : _this$schema.types;
      }
    }, {
      key: "rootQueryType",
      get: function get() {
        var _this$schema2, _this$schema2$queryTy;
        return _find(this.types, {
          name: (_this$schema2 = this.schema) == null ? void 0 : (_this$schema2$queryTy = _this$schema2.queryType) == null ? void 0 : _this$schema2$queryTy.name
        });
      }
    }, {
      key: "rootQueryFields",
      get: function get() {
        var _this$rootQueryType;
        return findRootQueryFields(this.types, (_this$rootQueryType = this.rootQueryType) == null ? void 0 : _this$rootQueryType.fields);
      }
    }]);
    return Schema;
  }();
  function findRootQueryFields(types, fields, path) {
    if (path === void 0) {
      path = '';
    }
    return fields == null ? void 0 : fields.reduce(function (fields, field) {
      var _type$metadata;
      var type = _find(types, {
        name: getFieldTypeName(field)
      });
      if (!type) {
        return fields;
      }
      if ((_type$metadata = type.metadata) != null && _type$metadata.type) {
        return fields.concat(_extends({}, field, {
          name: path + field.name
        }));
      }
      return fields.concat(findRootQueryFields(types, type.fields, path + field.name + "."));
    }, []);
  }
  function _findField(types, type, key) {
    var _key$split = key.split('.'),
      name = _key$split[0],
      path = _key$split.slice(1);
    var field = _find(type == null ? void 0 : type.fields, {
      name: name
    });
    if (!path.length) {
      return field;
    }
    return _findField(types, _find(types, {
      name: getFieldTypeName(field)
    }), path.join('.'));
  }
  function _getFields(types, field, fieldType, key, related, typeStr) {
    var _type$metadata2, _type$fields;
    if (key === void 0) {
      key = '';
    }
    if (related === void 0) {
      related = 3;
    }
    if (typeStr === void 0) {
      typeStr = '';
    }
    var type = _find(types, {
      name: getFieldTypeName(field)
    });
    if (!type) {
      return [];
    }
    if ((_type$metadata2 = type.metadata) != null && _type$metadata2.type) {
      var _field$metadata, _type$metadata3;
      typeStr = typeStr ? (_field$metadata = field.metadata) == null ? void 0 : _field$metadata.label : (_type$metadata3 = type.metadata) == null ? void 0 : _type$metadata3.label;
      if (! --related) {
        return [];
      }
    }
    return ((_type$fields = type.fields) == null ? void 0 : _type$fields.reduce(function (fields, subField) {
      var _type$metadata4, _metadata$label, _metadata$label2, _field$metadata2, _type$metadata5, _subField$type, _subField$type$ofType;
      var name = subField.name,
        _subField$metadata = subField.metadata,
        metadata = _subField$metadata === void 0 ? {} : _subField$metadata;
      var fieldText = !('group' in metadata) && !((_type$metadata4 = type.metadata) != null && _type$metadata4.type) && typeStr ? field.metadata.label + " " + ((_metadata$label = metadata.label) != null ? _metadata$label : name) : (_metadata$label2 = metadata.label) != null ? _metadata$label2 : name;
      var groupStr = typeStr || ((_field$metadata2 = field.metadata) == null ? void 0 : _field$metadata2.label);
      var fieldGroup = 'group' in metadata || !((_type$metadata5 = type.metadata) != null && _type$metadata5.type) && 'group' in (field.metadata || {}) ? metadata.group || field.metadata.group || Vue__default["default"].i18n.t('Fields') : false;
      var isOfTypeOrListOfType = Boolean(subField.type.kind === fieldType || ((_subField$type = subField.type) == null ? void 0 : (_subField$type$ofType = _subField$type.ofType) == null ? void 0 : _subField$type$ofType.kind) === fieldType);
      var isObjectType = Boolean(subField.type.kind === 'OBJECT');
      switch (true) {
        case isOfTypeOrListOfType:
          return fields.concat({
            value: "" + key + name,
            text: fieldText,
            group: fieldGroup ? (groupStr + " (" + fieldGroup + ")").trim() : groupStr,
            filters: metadata.filters,
            arguments: metadata.arguments,
            type: subField.type.kind
          });
        case isObjectType:
          return fields.concat(_getFields(types, subField, fieldType, "" + key + name + ".", related, typeStr));
      }
      return fields;
    }, []).sort(sortGroups.bind(typeStr))) || [];
  }
  function getFieldTypeName(field) {
    var _type$name, _type$ofType;
    var type = field == null ? void 0 : field.type;
    return (_type$name = type == null ? void 0 : type.name) != null ? _type$name : type == null ? void 0 : (_type$ofType = type.ofType) == null ? void 0 : _type$ofType.name;
  }
  function sortGroups(_ref, _ref2) {
    var groupA = _ref.group;
    var groupB = _ref2.group;
    if (!this || groupA === groupB) {
      return 0;
    }
    if (groupA === this || uikitUtil.startsWith(groupA, this) && groupB !== this) {
      return -1;
    }
    if (groupB === this || uikitUtil.startsWith(groupB, this) && groupA !== this) {
      return 1;
    }
    return 0;
  }

  var config = new Proxy({}, {
    get: function get(target, prop) {
      var _window$$yooessential, _window$$yooessential2;
      return (_window$$yooessential = window.$yooessentials) == null ? void 0 : (_window$$yooessential2 = _window$$yooessential.yoocustomizer) == null ? void 0 : _window$$yooessential2[prop];
    }
  });
  var SourceExtended$1 = /*#__PURE__*/function () {
    function SourceExtended(schema) {
      this.setSchema(schema);
    }
    var _proto = SourceExtended.prototype;
    _proto.setSchema = function setSchema(schema) {
      this.schema = new Schema(schema);
    }

    /**
     * Resolves the query name of a node or prop object
     */;
    _proto.getQuery = function getQuery(node, propName) {
      var _query, _query2;
      if (propName === void 0) {
        propName = '';
      }
      var query = this.getQueryObject(node, propName);
      if ((_query = query) != null && _query.inherit) {
        var _closest$source, _closest$source_exten;
        var closest = this.getClosestInheritableNode(node);
        query = (closest == null ? void 0 : (_closest$source = closest.source) == null ? void 0 : _closest$source.query) || (closest == null ? void 0 : (_closest$source_exten = closest.source_extended) == null ? void 0 : _closest$source_exten.query);
      }
      if ((_query2 = query) != null && _query2.global) {
        var _Dynamic$getGlobalQue, _Dynamic$getGlobalQue2;
        var id = query.global;
        query = (_Dynamic$getGlobalQue = _yooessentials.Dynamic.getGlobalQuery(id)) == null ? void 0 : (_Dynamic$getGlobalQue2 = _Dynamic$getGlobalQue.source) == null ? void 0 : _Dynamic$getGlobalQue2.query;
      }
      return query;
    };
    _proto.getQueryObject = function getQueryObject(node, propName) {
      var _this$getProp, _node, _node$source, _node2, _node2$source_extende;
      if (propName === void 0) {
        propName = '';
      }
      var query = (_this$getProp = this.getProp(node, propName)) == null ? void 0 : _this$getProp.query;
      if (query) {
        return query;
      }
      node = this.resolveAdjacentNode(node);
      return ((_node = node) == null ? void 0 : (_node$source = _node.source) == null ? void 0 : _node$source.query) || ((_node2 = node) == null ? void 0 : (_node2$source_extende = _node2.source_extended) == null ? void 0 : _node2$source_extende.query);
    };
    _proto.setQuery = function setQuery(node, name) {
      if (!this.isGlobalQuery(name)) {
        Source.setQuery(node, name);
        this.deleteQuery(node);
        return;
      }
      var query = {
        name: name
      };
      if (this.isGlobalQuery(name)) {
        query = {
          global: name
        };
      }

      // delete possible core query
      Source.setQuery(node, false);
      _yooessentials.set(node, 'source_extended.query', query);
      Vue__default["default"]["delete"](node.source_extended.query, 'arguments');
      this.setField(node.source_extended.query, false);
    };
    _proto.deleteQuery = function deleteQuery(node) {
      var _node$source_extended;
      if (!(node != null && (_node$source_extended = node.source_extended) != null && _node$source_extended.query)) {
        return;
      }
      Vue__default["default"]["delete"](node.source_extended, 'query');
      if (uikitUtil.isEmpty(node.source_extended)) {
        Vue__default["default"]["delete"](node, 'source_extended');
      }
    };
    _proto.isGlobalQuery = function isGlobalQuery(id) {
      return Boolean(_yooessentials.Dynamic.getGlobalQuery(id));
    };
    _proto.hasQuery = function hasQuery(node) {
      var _node$source2, _node$source_extended2;
      return Boolean((node == null ? void 0 : (_node$source2 = node.source) == null ? void 0 : _node$source2.query) || (node == null ? void 0 : (_node$source_extended2 = node.source_extended) == null ? void 0 : _node$source_extended2.query));
    };
    _proto.resolveAdjacentNode = function resolveAdjacentNode(node) {
      var _window$$yooess;
      var builder = (_window$$yooess = window.$yooess) == null ? void 0 : _window$$yooess.yoobuilder;
      if (builder != null && builder.exists(node)) {
        return node;
      }
      return (builder == null ? void 0 : builder.adjacentOf) || node;
    };
    _proto.isAdjacent = function isAdjacent(node) {
      var _window$$yooess2;
      return !((_window$$yooess2 = window.$yooess) != null && _window$$yooess2.yoobuilder.exists(node));
    };
    _proto.getProps = function getProps(node) {
      var _node$source3, _node$source_extended3;
      return _extends({}, ((_node$source3 = node.source) == null ? void 0 : _node$source3.props) || {}, ((_node$source_extended3 = node.source_extended) == null ? void 0 : _node$source_extended3.props) || {});
    };
    _proto.hasProps = function hasProps(node) {
      return !uikitUtil.isEmpty(this.getProps(node));
    }

    /**
     * Returns the prop object
     *
     * @param name string
     */;
    _proto.getProp = function getProp(node, name) {
      var _node$source_extended4, _node$source_extended5, _node$source4, _node$source4$props;
      return ((_node$source_extended4 = node.source_extended) == null ? void 0 : (_node$source_extended5 = _node$source_extended4.props) == null ? void 0 : _node$source_extended5[name]) || ((_node$source4 = node.source) == null ? void 0 : (_node$source4$props = _node$source4.props) == null ? void 0 : _node$source4$props[name]);
    };
    _proto.getPropDefaults = function getPropDefaults(node, query, propName) {
      var field = this.getPropField(query, propName);
      var isMultiSource = this.isMultiSource(node, query, propName);
      var isList = field.type === 'LIST';
      var fieldsets = ((field == null ? void 0 : field.fieldsets) || []).filter(function (_ref) {
        var multi = _ref.multi;
        return !multi || multi && (isMultiSource || isList);
      });
      return fieldsets.reduce(function (c, f) {
        return _extends({}, c, getFieldsDefaults(f.fields));
      }, {});
    };
    _proto.setProp = function setProp(node, name, prop) {
      var query = this.getQuery(node);
      var defaults = this.getPropDefaults(node, query, prop == null ? void 0 : prop.name);
      if (node.props) {
        Vue__default["default"]["delete"](node.props, name);
      }
      this.removeProp(node, name);
      _yooessentials.set(node, "source.props." + name, _extends({}, defaults, prop));
    };
    _proto.setExtendedProp = function setExtendedProp(node, name, prop, query) {
      var defaults = this.getPropDefaults(node, query, prop == null ? void 0 : prop.name);
      if (node.props) {
        Vue__default["default"]["delete"](node.props, name);
      }
      this.removeProp(node, name);
      _yooessentials.set(node, "source_extended.props." + name, _extends({}, defaults, prop));
    };
    _proto.removeProp = function removeProp(node, name) {
      var _node$source_extended6, _node$source_extended7;
      Source.removeProp(node, name);
      if (!(node != null && (_node$source_extended6 = node.source_extended) != null && (_node$source_extended7 = _node$source_extended6.props) != null && _node$source_extended7[name])) {
        return;
      }
      Vue__default["default"]["delete"](node.source_extended.props, name);
      if (uikitUtil.isEmpty(node.source_extended.props)) {
        Vue__default["default"]["delete"](node.source_extended, 'props');
      }
      if (uikitUtil.isEmpty(node.source_extended)) {
        Vue__default["default"]["delete"](node, 'source_extended');
      }
    };
    _proto.getClosestInheritableNode = function getClosestInheritableNode(node) {
      var _window$$yooess3;
      var Builder = (_window$$yooess3 = window.$yooess) == null ? void 0 : _window$$yooess3.yoobuilder;
      node = this.resolveAdjacentNode(node);
      do {
        var _node3, _node3$source, _node4, _node4$source_extende;
        node = Builder == null ? void 0 : Builder.parent(node);
      } while (node && !((_node3 = node) != null && (_node3$source = _node3.source) != null && _node3$source.query || (_node4 = node) != null && (_node4$source_extende = _node4.source_extended) != null && _node4$source_extende.query));
      return node;
    }

    /**
     * Returns the query field name
     */;
    _proto.getField = function getField(query) {
      var _query$field;
      return (query == null ? void 0 : (_query$field = query.field) == null ? void 0 : _query$field.name) || '';
    };
    _proto.setField = function setField(query, name) {
      if (name) {
        _yooessentials.set(query, 'field', {
          name: name
        });
      } else {
        Vue__default["default"]["delete"](query, 'field');
      }
    }

    /**
     * Returns a list of fields for the queried source
     */;
    _proto.getFieldsList = function getFieldsList(query) {
      var field = this.getSourceField(query);
      return this.schema.getFields(field);
    }

    /**
     * Returns the source schema field
     */;
    _proto.getSourceField = function getSourceField(query) {
      var field = this.getField(query);
      var source = (query == null ? void 0 : query.name) || '';
      var path = field ? source + "." + field : source;
      return this.schema.findField(path);
    };
    _proto.getPropField = function getPropField(query, propName) {
      var fields = this.getFieldsList(query);
      var propField = fields.find(function (field) {
        return field.value === propName;
      });
      if (!propField) {
        return null;
      }
      return _extends({}, propField, {
        fieldsets: [{
          label: 'Arguments',
          fields: resolveArgumentsFields(propField)
        }, {
          label: 'Filters',
          fields: resolveFiltersFields(propField)
        }, {
          label: 'Implode',
          multi: true,
          fields: resolveImplodeFields()
        }]
      });
    };
    _proto.getQueryOrigin = function getQueryOrigin(node, propName) {
      var _Vue$events$trigger;
      return (_Vue$events$trigger = Vue__default["default"].events.trigger('yooessentialsSourceQueryOrigin', {
        node: node,
        prop: propName
      })) != null ? _Vue$events$trigger : '';
    };
    _proto.isMultiSource = function isMultiSource(node, query, propName) {
      var _this$getSourceField, _this$getSourceField$;
      var queryOrigin = this.getQueryOrigin(node, propName);
      if (queryOrigin != null && queryOrigin.match(/node/)) {
        return false;
      }
      return ((_this$getSourceField = this.getSourceField(query)) == null ? void 0 : (_this$getSourceField$ = _this$getSourceField.type) == null ? void 0 : _this$getSourceField$.kind) === 'LIST';
    };
    return SourceExtended;
  }();
  function getFieldsDefaults(fields) {
    var defaults = {};
    for (var _iterator = _createForOfIteratorHelperLoose(fields), _step; !(_step = _iterator()).done;) {
      var field = _step.value;
      if ('default' in field) {
        _yooessentials.set(defaults, field.name, field["default"]);
      }
    }
    return defaults;
  }
  function resolveArgumentsFields(field) {
    return prepareFields(field.arguments, 'arguments');
  }
  function resolveFiltersFields(field, defaultFilters) {
    if (defaultFilters === void 0) {
      defaultFilters = ['before', 'after', 'search', 'replace'];
    }
    var baseFilters = config.sources.filters;
    return [].concat(defaultFilters, prepareFields(field.filters, 'filters')).map(function (name) {
      return uikitUtil.isString(name) ? _extends({
        name: "filters." + name
      }, baseFilters[name]) : name;
    }).filter(function (t) {
      return t;
    });
  }
  function resolveImplodeFields() {
    return prepareFields(config.sources.implode, 'implode').filter(Boolean);
  }
  function prepareFields(fields, prefix) {
    return uikitUtil.isPlainObject(fields) ? map(fields, function (field, name) {
      return _extends({
        name: prefix + "." + name
      }, field);
    }) : fields || [];
  }

  uikitUtil.assign(window.$fields, fields);
  Vue__default["default"].component('yooessentials-source-queries', __vue_component__$8);
  var Dynamic = new Vue__default["default"](DynamicModel);
  var SourceExtended = new SourceExtended$1();

  // expose globally
  window.$yooess.Source = Source;
  window.$yooess.SourceSchemaModel = Schema;
  window.$yooess.Dynamic = Dynamic;
  window.$yooess.DynamicBuilderPanel = DynamicBuilderPanel;
  window.$yooess.SourceExtended = SourceExtended;
  window.$yooess.SourceExtendedModel = SourceExtended$1;
  Vue__default["default"].events.on('initYooess', function (e) {
    SourceExtended.setSchema(window.$yooess.yoocustomizer.schema);
  });

})(Vue, UIkit.util, $yooessentials, $fields);
